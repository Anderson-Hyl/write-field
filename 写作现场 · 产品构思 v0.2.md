# 写作现场 · 产品构思 v0.2

> 面向 markdown 的长文写作。打开像 Pages：中间一张白纸，稿在上面；AI、草稿、资料围在周围。
> 一句话定位：**AI 是写作的助手，不是主人。**
>
> 状态：草稿 / 待打磨。带 `【待定】` 的条目是还没想清楚的，带 `【已定】` 的是这轮讨论里已经钉死的。
> 起源：从「为 markdown 写作加 AI 功能」出发，经过对 Flowise（范式错位）、Maybe（结构性无解）、
> 以及 Mosaic 自身交互的复盘，收敛到下面这个形态。

---

## 一、第一性判断【已定】

**写作的价值在过程，不在成品。** 手写让我们重新审视自己写过的东西，并在过程中撞出新灵感 —— 这是纯 AI 生成无法替代的。

由此得到这个产品的地板规则【修订，2026-09-17】：

> **AI 是助手，不是主人。进不进正文，完全由作者决定。**

旧句「任何替你写的功能都不做」太死：分析完一张图，再手抄 mermaid 进稿，是在惩罚使用。图表、表格、一段候选，都可以是 AI 的产出——但必须先停在周围的卡上。**没有作者手势，正文一个字、一块图都不会变。**

推论：

- AI 的产出是 **问题、批注、资料、图表、表格、候选片段**，形态是卡，不是直接改纸。
- 作者可以把卡插入正文（图表、代码块、表格），也可以自己改写成句子，也可以只当 footnote。插不插、插成什么样，是作者的命令。
- AI 接口里仍然没有「自己改这篇」：不能定时写入，不能在分析完之后自动落稿。

第二条判据（用来砍功能）不变：

> 如果一个功能在你**不写作**的时候也有用（总结、生成、问答），那它不属于这个产品。

---

## 二、范围界定

### 在范围内【已定】

- 手写正文的一切：编辑、结构、引用、出处记账
- AI 作为助手的动作：**审**、**忆**、**构**、**查**，以及应作者要求做出可插入的块（图表 / 表格 / 片段）——先落在卡上
- 资料与反馈停在**纸的周围**（临时内容，不是无限画布，也不是全局侧栏）
- 出处记账：这段来自哪、引了哪句、footnote 挂在哪

### 明确排除【已定】

| 排除项 | 理由 |
| --- | --- |
| **内容真伪的验证** | 真实性只能由作者负责，工具不扮演裁判。AI 判断真伪会制造虚假安心 |
| 自动写入 / 自动改写正文 | 助手不能当主人。分析完不自动落稿。Pages Writing Tools 那种 Replace 仍禁止 |
| 未经作者手势的插入 | 插入图表、表格、片段是**作者**的命令（拖入 / 确认套用）。AI 没有这条命令 |
| 多人协作 / 云端账户 | 单人可持续性（见 §八） |
| 自建内容/题库/语料 | 那是别人的生意 |

**保留但降级的一项**：原设计的"主张账本五状态"（未验证/有来源/来源矛盾/找不到来源/我的判断）中，涉及真伪判断的部分全部删除。**卡片状态只剩四种**【已定】：

```
草稿 → 待用 → 已用
  ↘  废弃  ↗
```

---

## 三、产品形态【已定方向，2026-09-17】

**打开之后像 Pages：中间一张白纸，主体内容在纸上；AI、草稿、资料、求证这些临时内容围在周围。**

单位仍叫 Topic。这不是 Mosaic 的无限房间，也不是「正文 + 全局资料侧栏」，也不是左栏编辑器 + 右栏检查器。

Pages 借三件：**打开是纸**、**没有第二台世界相机**、**文稿的专业观感**（白纸、衬线正文、系统灰底、周围像批注而不是知识图谱）。不借格式检查器，不借 Writing Tools 改正文。

S1 第一版把纸做成暗色浮块、卡做成绿/紫圆角块、线做成节点端口——那是小众写作 / PKM 的样子，**不要**。纸永远是白的；卡是白底细边、系统字号；线 1pt、低对比。正文用系统衬线（New York）。暗色模式只改窗口底，不把稿纸涂黑。

「临时」指的是**不是这篇正文**——它们先停在周围，进纸必须手搬。不是说隔夜可以丢：现场（光标、当时在周围的卡）要能恢复。

- Mosaic 的 Room 在这里试着用 **Topic**（一篇稿、一条线索、一个题目）。不要在 UI 上写 Room。
- App 就是容器。没有绑磁盘的 Workspace。v1 文稿列表可以是侧栏，但侧栏不是写作现场。
- 换 Topic = 换一篇纸和它四周的卡。Soundtrack 仍是整 app 一首（v1）。
- **正文永远是线性的**，写在正中那张纸上，不能把段落拆散摊开。纸**纵向**滚；横向留给周围的临时内容。
- 周围是临时工作记忆（AI / 草稿 / 资料 / 求证），不是第二份稿，也不是可以缩走的宇宙。

**分叉（这条比「像 Mosaic」更硬）：** Mosaic 的 tile 对等；这里有一个特权表面——主正文。设计稿如果把编辑器画成会缩走的普通 tile，或把纸放在一张可平移的无限平面中央，都是在画错产品。

### 3.0 对「做成 Mosaic」的反方【要留着】

1. **Mosaic 的 Room 有两件事，不要捆在一起。** 高频切换（终端 ↔ 浏览器）写作里确实少，所以侧栏不必常驻。**切分**写作里同样需要：去年的卡和今天的句子不该挤在一起——一篇一张纸就够。反对的是「chrome 做成 Mosaic」，也反对「一篇稿铺成无限平面」。
2. **Topic 这个词比现在的表更大。** 一行 `topics` + 一个 `body`，就是文章。Topic 听起来像「一条研究线索，稿可以换、卡还在」。要么承认 v1 就是文章、侧栏是文稿列表；要么以后真让一个 Topic 容纳不止一份稿。现在两个都要，名字会骗人。
3. **§3.2 还在：空间不是产物。** Mosaic 把工作铺在二维上，切换本身就减少了。写作里空间只为驻留和恢复服务。壳画得越像 Mosaic，越容易为了摆齐 tile 而忘记回正文。
4. **「先把页面做像再实现」有范围。** 壳的一张结构草图有用，七张抛光板会拖住唯一能杀死产品的测试（IME、手势、两周真写）。Mosaic 的设计板是在改一个已经能用的 app。

### 3.1 周围真正解决的问题：上下文驻留【已定】

周围不需要证明二维关系“不可替代”。它只需要显著降低写作过程中维护上下文的成本。

复杂写作会不断产生两类上下文：

1. **信息上下文**：网页、论文、PDF、原始引用、自己的旧文章、AI 找到的候选资料。
2. **思考上下文**：我为什么怀疑这一段、AI 给过什么反驳、还有什么问题没回答、某个论点从哪里来、为什么放弃过一条路。

传统工具把这些内容分散在浏览器 tab、聊天记录、笔记软件和脑内短期记忆里。每次切换都需要重新寻找、重新定位、重新回忆“我为什么来到这里”。

> **这个产品的目标，是让写作所需的上下文驻留在文章周围。**

因此周围更接近一个**外部工作记忆**，而不是一块用于整理漂亮结构图的白板。重新打开时，看到的不只是这篇稿，而是**上一次离开时的写作现场**：纸，加上当时围着的临时内容。

### 3.2 空间不是目的【已定】

**空间布局本身不是产物。** 这一点与 Mosaic 不同：Mosaic 中工作上下文被铺在二维空间里，本身就能减少 workspace 切换；写作里，空间只有在它帮助参照、恢复和收敛时才有价值。

判断周围是否成立，不再要求“某种关系只能二维表达”，而看三个更实际的问题：

1. 它是否减少了为了找资料、找 AI 回答、找旧思路而离开当前写作现场的次数；
2. 它是否减少了“我刚才为什么打开这个 / 我当时在想什么”的重建成本；
3. 它是否最终帮助作者回到线性正文，而不是诱导作者沉迷整理。

换句话说：**周围的价值是上下文驻留 + 结构 → 散文的转换，而不是空间本身。**

### 3.25 正文纵向，横向留给周围【已定方向，2026-09-17】

两条轴各做一件事：

| 轴 | 用来干什么 |
| --- | --- |
| **纵向** | 稿。段与段、上一句和下一句。手指往上滑，这篇在走。 |
| **横向** | 周围。这段的资料、AI、草稿、求证挂在纸的左 / 右。 |

**不要横向翻页。** 横滑若用来换「下一张纸」，会和两侧的临时内容抢同一条轴：要么周围被当成翻页手势吃掉，要么翻页时周围整页清掉，这段旁边的记忆就断了。长文写作里，看不到上一段，本身就是一次上下文切换——产品要减少的正是这种切换。

Pages 的文字处理也是**纵向走**：纸一张张往下叠，不是左右翻书。左右翻是 Keynote、绘本、版式页。本产品是源码模式的一篇 markdown，没有版式引擎，更不该先发明「第 3 页」。

稿在脑子里也是一条线，不是一叠幻灯片。CodeMirror 的文档就是纵向流；周围按段落 Y 跟走，只有纵向滚才对得上。

看起来像不像 A4 一张张叠下来（Pages 那种纸影），原型不做。那是版式，不是写作现场。第一刀：正中一列连续的纸，往下写、往下滚。

### 3.3 一张纸装什么【方向，未钉死】

**全 app 一张无限平面：视觉上会出问题。** 不是审美，是工作记忆被撑破。无关题目的卡、线挤在同一平面上，缩出去是垃圾场，跳转变成唯一导航。Mosaic 用 Room 切开的就是这件事。写作即使很少「换房间」，也需要这层切分——一篇稿一张纸就够，不必看起来像 Mosaic，更不必在纸后面再铺一层宇宙。

**一篇一张纸，卡变多了，靠「钉住 + 屏幕外收起」，不靠卡跟着光标飞，也不靠缩远。**

卡钉在段落上（锚是真的）。滑动正文时，锚点不在当前屏幕里的卡收起——这就是 working set，不是把卡变成跟光标走的浮层，也不是第二台相机。已用 / 废弃可以叠得更扁。不靠作者手工收拾，也不靠再开一张平面。

**默认可见形态：卡挂在纸的两侧，连到这段的侧边，不进字缝。【已定方向修订，2026-09-17】**

2026-09-16 把这个收成「gutter 上 6px hub，点开才出卡」。那是 Mosaic 的静默成员，指针可以，**手指不行**。iPad 上要再点一次才看见这段的资料，现场就慢了。

现在的默认（iPad 优先，Mac 同一套）：

- **正在写的这段：** 卡已经在纸的左 / 右，连到这段的侧边。不用先点开。这是速度快的那一拍。
- **别的段：** 侧边最多一个够手指点的端口。没有卡的段，侧边什么都没有。
- **滚出屏幕：** 卡收起。不是第二台相机，是这篇在滚。
- **hover：** 只在 Mac 上当额外披露，**不是打开，也不是 iPad 路径。**

连线从**纸的侧边**接到卡，不从字缝里的点接到右栏。卡可以在两侧；不要收成「左编辑器 + 右检查器」。

展开集合仍然进 `scenes.openCardIDs`（正在写的这段算打开）。静默端口可以重算，摊开的卡不能丢。

### 3.4 一段几个锚点：库里按句子，画面上按段【已定方向，2026-09-16】

同一段里对这句话提问、给那个短语挂资料，是真会发生的。那不是「这段有 N 个 hub」。

| 层 | 粒度 |
| --- | --- |
| **存储** | 继续用 TextQuoteSelector。`exact` 可以是一句 / 一个短语。同一 block 上可以有多行 `anchors` |
| **静默** | **每个可见 block 最多一个侧边端口**（纸的左或右，在该段 Y）。够手指点，不是字缝里的 6px |
| **打开** | 正在写的这段，卡已经在两侧。点端口 = 看别的段。双向高亮再去点亮 `exact` 那句 |
| **真正长在字上的** | 只有已经进 markdown 的东西：footnote、§6.2 的「标为待查」。周围的卡不进字缝 |

不要做成：

- **在文字位置插点。** 和选区、IME、阅读抢同一条带子；中文段尤其密。源码模式里「字的位置」还会随折行跳。
- **横向列表当第二套 chrome。** 那只是 N 个端口排成一行。打开之后卡多了，绕着**这段**摊在两侧。
- **为「段落端口」新建一张表。** 渲染时按解析出的 block 把卡分组。端口是视图，不是新实体。

创建手势：空段落，**这段的侧边就是把手**（拖出去：问 / 丢资料）。已经有卡的段落，拖的也是这个侧边，不要在字旁边再放一只手。iPad 没有 hover，不能靠指针出现把手——侧边要一直够得着。

还没钉死：左右有没有习惯分工（草图里资料在左、求证在右，先当草稿）；一段同时几张卡；Mac 要不要比 iPad 更静。

### 3.5 一次写作怎么走下来【打通，细节未锁】

前面几节拆开了壳、侧边、一段多卡、卡再长出卡。它们不是四张页面。同一块现场按时间走下来，才是产品：

1. **开题。** 打开就是一张在正中的白纸。没有点，没有卡。开始打字。
2. **写到起疑。** 某段不确定。这段侧边是把手。拖出去：问 AI，或把一句资料丢进来。
3. **停在旁边。** 问题 / 回答 / 资料出现在纸的左或右，连到这段的侧边。正文还在打，东西没有进字里。
4. **对资料再想。** 已经在旁边的卡上再问、再拆、再反驳。新卡仍属于这段。关系要能找回「从哪张来」——怎么画（短颈、出处、线、簇）跟整场走，本轮不锁。
5. **搬回正文。** 某句要用了：自己写成句子、拖成 footnote，或把图表 / 表格 / 片段**插入纸上**。卡变为已用。**到这里循环才闭合**——前面都是驻留，这里才是收敛。插入是作者点的，不是分析完自动落的。
6. **往下写。** 滚这篇。刚才那段的卡收起，侧边留下一个端口。不是换房间。
7. **只想打字。** 专注模式：卡让开，点还在。写完回来，刚才摊开的还在。
8. **隔夜。** 打开还是这个 Topic。光标、当时摊开的卡、未回答的问题还在。看到的是昨天的现场，不是一篇干净文稿。

这一圈要通的是：**打字 → 起疑 → 东西停在旁边 → 再想 → 写进正文 → 现场自己收 → 明天还能接着。** 任何一截如果要另开一张页面、另做一套 chrome，就是断的。

还没串上、比短颈好不好看更要紧的关节：

- 空段落第一次从侧边拉出，和已经有卡之后再拉，是不是同一只手。
- 资料先扔进来、还没挂段，怎样接到第 3 拍，而不变成一块研究白板。
- 第 5 拍（搬运）现在比第 2–4 拍薄。没有这一拍，驻留只是堆积。
- 写字时卡开着：只有这篇在滚，卡跟这段的 Y，不要出现第二台相机。
- iPad 上同一圈必须快：侧边够手指，正在写的这段卡已经在，不靠 hover。

结构草图：`Design/一次写作.html`。派生怎么画、药丸还是短颈，等这一圈能走完再看。

### 3.6 不是什么【已定方向，2026-09-17】

| 形态 | 相机 | 临时内容在哪 | 判词 |
| --- | --- | --- | --- |
| 无限画布（Heptabase / Obsidian Canvas / Mosaic Room） | 平移 + 缩放 | 世界坐标，纸是其中一张 tile | **不采用** |
| 左栏编辑器 + 右栏检查器 | 正文滚动 | 全局一列，不认段落 | **不采用**（还是工作台） |
| 正文 + 全局侧栏 | 正文滚动 | 所有卡在同一列 | 周围失败之后的降级 |
| **中间一张纸，周围是临时内容** | **只有这篇在滚** | 挂在当前这段的两侧 | **采用** |

Mosaic 要空间化的是对等的工作上下文。这里只有一个特权表面——稿。把稿放进无限平面，或收成 IDE 分栏，第一件事都变成导航界面，而不是打字。

没有第二台相机：不平移、不缩放、不铺点阵。纸不能当成可拖的 tile。草图里的格子和八角选框是绘图工具，不是产品。

iPad 要快：侧边够手指；**正在写的这段，周围已经在**；左右都能用，不要逼进右栏。

还没钉死：左右有没有分工；iPad 竖侧边变窄之后怎么放；线是短曲线还是 circuit。这些是细节，不是形态。形态已经够用：正中一张纸，周围是临时内容，纵向滚这篇。

### 3.8 旁注怎么用颜色【已定方向，2026-09-17】

种类要用颜色分开（资料、待决、助手产出、反驳），但**整张卡不能变成色块**。左侧 2pt 色条也太像十年前的后台边框，不用。

颜色放在三处，卡本身仍是白的：

1. **符号**（系统 SF Symbol，hierarchical），caption 同色。
2. **接到这段的线，和侧边的点**——种类长在关系上，这是这篇稿才有的，不是 CMS 面板。
3. **一点色光**：选中或悬停时，影子带该色，约 15% 不透明度。不是铺底。

不要铺满，不要每种 AI 动词一种霓虹。种类仍要写在字上。

**进行中的 Glow（§3.9）是状态，不是种类。** 算完光要熄；熄了之后才露出符号和线的颜色。

四种角色就够，不要为「验证 / 生成资料 / 检视 / 图表」各开一色——那些都是助手产出：

| 角色 | 用在 | 色 |
| --- | --- | --- |
| **资料** | 剪藏、检索来的出处 | 系统蓝 `#007AFF` |
| **待决** | 问题、待查 | 克制的琥珀 `#C47B17` |
| **助手产出** | 回答、检视、AI 找来的资料、待插入的图表 | 一条冷灰青 `#5B7C8A` |
| **张力** | 反驳 | 克制的红 `#C4474A` |

「AI 验证」如果还没结论，走**待决**；有了检视文字，走**助手产出**。不要第五种「验证紫」。

后期玻璃（`card-design.mp4`）可以叠在白卡上当材质，**不要改这套色条**。不进 S1 的玻璃；色条现在就可以用。

### 3.9 AI 进行中长什么样【已定方向，2026-09-17】

重度用 AI，进行中必须看得出，而且**绝不能长得像 Writing Tools 在改正文**。

参考：`Design/ai-orb.mp4`（两颗玻璃球可分可合，内部虹彩在流）；Apple Intelligence Glow（蓝 / 紫 / 珊瑚 / 琥珀的角向渐变，绕着**正在被 AI 处理的那块表面**转）。

| 状态 | 视觉 | 落在哪 |
| --- | --- | --- |
| 安静写 | 没有光 | 纸是普通白纸 |
| 思考 / 流式 | 角向虹彩绕边转 | **正在长出来的那张卡**，或侧边端口上的一颗小球 |
| 落地 | 光熄掉，卡变成普通玻璃卡（§3.8） | 周围，不进正文 |
| 失败 | 不要虹彩 | 卡上可见的失败，runtime only |

**借用**

- Glow 表示「AI 正在干活」，只包住周围的对象。
- 气泡：侧边端口上先出现一颗球（思考），再展开成卡（落地）。像 `ai-orb.mp4` 里两球合到一颗再分开——问和答可以共用这一拍，不要做成桌面上的吉祥物。
- 光是 runtime。不把「正在请求」写入 `scenes`（§8.2 注意力层已经这么定）。
- Reduce Motion：跳过球和转光，直接出现卡。

**不要**

- **不要给正文、选区、整张纸套 Glow。** 那是 Writing Tools 的「我在改这段」。本产品禁止 `body.replace`，视觉上也不能像。
- 不要屏幕一圈 Siri 光。那是系统助手，不是这段旁边的读者。
- 不要写字时旁边一直漂着一颗球。安静是生存条件（§6.3）。
- 不要为效果在 S1 上做。跟 S6 一起：第一次真的提问时，卡必须带着进行中状态出现。

调色：周围可以用接近 Apple Intelligence 的虹彩，让人认出「在算」。纸上永远不要出现这圈颜色。

### 3.7 Pages 的 AI 不是这个产品的 AI【已定方向，2026-09-17】

形态靠近 Pages 之后，要先问：Pages 自己是不是已经能「AI 协同写作」？能的话，再做一个白纸应用，默认更差。

取证见 `research/pages-ai.md`。结论先说：

**Pages 有 AI，而且很深；它做的是本产品明确禁止的那件事。** 协同在 Pages 里是人与人合写，不是 AI 当读者。

| Pages 实际有的 | 和本产品 |
| --- | --- |
| Writing Tools：校对、改写、摘要、Compose（ChatGPT 直接写进文档） | AI 自己改纸，仍禁止。作者从卡插入图表 / 片段，不是这条 |
| Siri / ChatGPT 问「这篇在说什么」 | 近：AI 在读。远：回答在聊天气泡里，不驻留，默认还可 Copy 进正文 |
| 评论 / 修订窗格 | 近：纸旁边有字。远：人的审稿批注，不是资料、问题、出处 |
| iCloud 多人实时合写 | 本产品 §二排除 |

所以「我们肯定不如它好用」只在这句成立：**如果我们也去做改句子、写段落、版式、合写。** 那不用做，Pages 已经免费做好了，还接在操作系统上。

**轻量是对的，轻的是「不是 Pages」。** 没有版式引擎、没有模板、没有多人、不接 Writing Tools 的改写 / 生成。不是把地板规则也削薄——没有「AI 当读者 + 产物停在段旁」，这个应用没有存在的理由，轻量也救不了。

不要做的（形态像纸也不做）：

- 不接系统 Writing Tools 的 Rewrite / Compose。OS 会提供 API（WWDC25 coordinator），接了就是在 Pages 的动词上比赛。
- 校对（Proofread）是灰区：它也 Replace 原文，而且会改意思（iA Writer 2026-09 的记录）。原型不接。
- 不把 Siri 问文档当成「已经有驻留」。气泡关掉，现场就没了。

还没钉死：系统校对将来要不要以「建议卡」的形式出现（产物进四周，不 Replace）。那是 S6 之后的事，而且必须先证明它不是改写的马甲。

---

## 四、核心机制（灵魂）【已定】

> **AI 的产物永远先落在纸的周围；进正文必须是作者的手势。**

周围是减速带。回答、图表、建议、资料，全部先变成纸旁边的对象。作者可以：自己改写成句子、插入这块（图表 / 表格 / 片段）、或只挂 footnote。没有作者手势，纸不变。

这条规则现在有两层含义：

1. **认知减速**：避免一句“看起来不错的话”未经理解就变成“我的话”；
2. **上下文驻留**：AI 的输出不是一次性聊天记录，而是当前写作现场中的上下文对象，应该留在它所针对的段落附近，之后仍能被重新发现、比较和引用。

这同时满足三个条件：
1. 正文的每一次改动都是作者的决定（手写，或从卡插入一块）；
2. AI 输出始终先是待审阅对象，不是已经进稿的成品；
3. 与当前文章有关的思考不会因为聊天时间线继续滚动而消失。

传统聊天天然是 `Q → A → Q → A` 的时间线，但写作中的关系并不按时间发生：一个回答可能针对第三段，一条资料可能支持第一段，一个反例可能几天后才被重新启用。周围允许这些对象**停留在它们所属的段落旁边**。

它也是 Flowise 那条教训的正面应用：**AI 的输出不是终点，是待审阅、待安置、待吸收的对象。**

---

## 五、对象模型

### 5.1 纸上与周围的对象

| 对象 | 谁产生 | 字段 | 与正文的关系 |
| --- | --- | --- | --- |
| **正文（纸）** | 作者 | `topics.body`（干净 markdown） | 本体 |
| **资料卡** | 作者剪藏 | 出处（URL/文件）、标题、作者、日期、**选中的原句**、**"为什么留着它"（一句自述）** | 可被引用 |
| **问题卡** | 作者（手写 / 从正文拉线） | 问题原文、产生方式、状态 | 指向某段 |
| **回答卡** | AI | 来源模型、时间、内容、是否已搬运 | 属于某问题卡 |
| **碎片卡** | 作者 | 一句话 | 待搬运 |
| **章节卡** | AI 提议 / 作者调整 | 标题、包含的段落引用、顺序 | 组成正文结构 |
| **批注卡** | AI | 类型（反驳/隐藏前提/模糊词/节奏）、针对段落、内容 | 指向某段 |
| **废弃卡** | 作者 | 原内容、**为什么放弃** | 不进入正文，但保留 |
| **边（连线）** | 双方 | 类型、源、目标 | 见 5.2 |

【待定】废弃卡的价值很高（走错的路是资产的另一半），但它会不会只是心理安慰？需要真实使用来判断。

### 5.2 边的类型【待定 → 倾向已定】

| 边类型 | 含义 |
| --- | --- |
| 引用 | 正文的 footnote ↔ 资料卡 |
| 依据 | 这段论断依赖这张卡 |
| 反驳 | 这张卡反对这段 / 这张卡反对那张卡 |
| 待解决 | 问题卡 ↔ 段落 |
| 派生 | **这张卡是从那张卡长出来的**（资料 → 问题/批注/碎片）。吸收进正文仍走依据 / 引用，不再单开一种「卡 → 段落」的派生 |

**每根线一画出来就持久存在、并且带类型**，理由：
1. 它是动作的**痕迹**（三个月后知道这里为什么有张卡）；
2. 它是结构的**来源**（边一多，文章结构自己浮现）；
3. 它是**双向高亮**的实现基础。

---

## 六、核心交互：从正文拉出一根线【已定方向，细节待定】

主力手势：从**这段纸的侧边**拉出临时内容。借鉴 Mosaic 的「从把手拖线」，但把手在纸边，不在 tile 标题栏。

### 6.1 手势规格

| 阶段 | 行为 |
| --- | --- |
| 起手 | 这段的**纸侧边**就是把手。拖 = 问 / 丢资料 / 再连一张。点端口 = 看不是正在写的段。**必须在字缝外面**，否则和选区打架。iPad 上侧边一直在，不靠 hover |
| 过程 | 空白处高亮 = 可发起动作；已有卡片高亮 = 可建立关系 |
| 松手 | 弹出动作（高频 3 个 + 更多） |
| 加速 | **marking menu**：熟练后朝方向甩出即触发，不必看菜单（写作者不必离开键盘节奏）|
| 键盘等价 | **必须全套存在**。手势是快捷键的可视化，不能是唯一入口 |
| 撤销 | 落在空白处是误触重灾区，必须有取消区 + 一键撤销 |

### 6.2 松手后的动作

| 类别 | 动作 | 产物 |
| --- | --- | --- |
| 高频 | 问 AI（就这段提问）| 问题卡 + 回答卡 |
| | 找资料（**先搜我自己的语料**，再外部）| 候选资料卡 |
| | 标为待查 / 待办 | 行内标记 |
| 关系 | 建立引用（→ 资料卡）| footnote ↔ 卡双向链接 |
| | 标记依据 / 标记反驳 | 带类型的边 |
| 结构 | 拆成卡片（"这段是三个论点"）| 碎片/章节卡 |
| | 挖隐藏前提 / 最强反驳 | 批注卡 |
| 合并 | 把几张卡合成一段 | 收敛路径 |
| **不做** | 自动续写 / 自动润色 / 自动改写正文 | ❌（可产出候选卡；套用是作者的事）|
| | 给资料写摘要并自动当正文 | ❌（对资料只 审 / 问 / 拆；要进纸，作者自己插入） |

### 6.3 其他必要交互

| 交互 | 说明 |
| --- | --- |
| **双向高亮** | 光标在正文 → 相关卡片亮；点卡片 → 正文相关段落亮。核心作用是让上下文和正文之间保持可见的归属关系 |
| **Working Set / 注意力层** | 正在写的这段，卡已经在两侧。别的段只留侧边端口。滚出屏幕则收。不靠卡跟着光标飞 |
| **恢复现场** | 重新打开文章时优先恢复上一次正文位置、当时**展开的**卡片（`openCardIDs`）和未解决问题。静默标记可以重算，展开集合不能丢 |
| **拖入 / 套用** | 卡拖进纸：图表、表格、代码块可以**按作者选择插入**；资料默认变成 footnote（出处不丢）。替换已有段落【待定】，原型先做「插入一块」，不做选区 Replace |
| **拆 / 合** | 正文一段 → 拆成卡片；多卡 → 合成一段。收敛动作必须显式存在 |
| **专注模式** | 只剩纸。周围让开，侧边端口还在。**这是生存条件，不是可选功能** |
| **安静** | 不主动弹、不打断。正在写的这段，周围已经在；别的段只留端口 |

### 6.4 卡再长出卡【已定方向，2026-09-16】

会有这种现场：先丢几张资料卡，再让 AI **处理这些资料**——不是改正文，是从卡上再问、再拆、再反驳。新卡必须能看见它是从哪张资料来的。

侧边 → 卡只回答「属于这段」。**回答不了「这张是从那张长出来的」。** 关系仍入库（`edges`）。箭头不是默认方案；常画第二层长线也容易和纸边的线抢。**具体怎么呈现（簇、出处、按需墨水）不在这里锁**，跟 §3.5 那一圈一起看。

规则：

1. **手势可以从卡的把手拉出**，不只从纸的侧边。松手在空白处 = 对这张卡发起动作（问 / 拆前提 / 最强反驳）；松手在另一张卡上 = 只连边，不跑 AI。
2. **新卡继承来源卡的锚点**（同一段）。多张资料一起处理，新卡挂当前这段，并对每张来源各**写入**一根派生边。画不画、怎么画，见 §3.5。
3. **没有锚点的资料可以先待着**（剪藏 inbox）。但 AI 不能在一堆没挂段落的卡上自己跑——那是研究白板，不写作时也有用，不属于这个产品。先把资料拖到某段的侧边，或对「当前段 + 这些卡」提问。
4. **一次手势仍有产物上限。** 不允许「把这十篇 PDF 处理成一张网」。
5. 回答卡继续走 `parentCardID`（一问一答）。多来源、再拆、再反驳走 `edges`，不要把 `parentCardID` 加成多父。

原型仍可以只做「就当前段落提问」。卡上的把手和派生边是同一套数据，S6 之后接上，不另开产品。

---

## 七、AI 动作清单

### 做

| 动作 | 产出 | 备注 |
| --- | --- | --- |
| 就当前段落提问 | 回答卡 | 最核心的一个 |
| 最强反驳 | 批注卡 | 逼补论证 |
| 挖隐藏前提 | 批注卡 | "你默认 X 成立但没说" |
| 模糊词体检 | 批注卡 | 把"更好/大概/合理"逐一点名 |
| 反推提纲 + 重心检查 | 视图 | 看森林，不改树 |
| 拆段 / 合段 | 卡片 | |
| 找资料 | 候选资料卡 | **先自己的语料，后外部** |
| 跨文重复 / 矛盾检测 | 引用列表 | 需要语料索引，长期护城河 |
| 未完成念头清单 | 视图 | 所有 TODO / "回头再想" 聚成一池 |

### 不做

- 直接生成正文段落
- 自动修改正文
- 判断内容真伪
- 编造引用（**架构级禁止**：引用对象只能来自真实检索结果或作者剪藏；"没有来源"就显示没有）

### 交互顺序原则【已定】

> **先让我怀疑，再让机器核对。**

- 默认安静：AI 在后台准备，不主动扫全篇、不弹卡片
- 作者标记"这里我不确定" → AI 才去查（这个动作本身就是在练习自我怀疑）
- 可选"重要文档"模式自动跑，但结果只进周围，不打断写作、不进正文
- 反模式：每次写完自动全篇检查 → 作者会逐渐不再自己核对

---

## 八、与 Mosaic 的关系

### 8.1 更深层的共同点【已定】

两者表面上服务不同场景，但背后解决的是同一个问题：

> **不要让我为了处理另一个上下文，而离开当前上下文。**

- Mosaic 空间化的是**工作上下文**：终端、浏览器、agent、不同任务现场。
- 写作现场空间化的是**思考上下文**：资料、疑问、反驳、AI 回答、未完成线索以及它们和正文的关系。

因此这不是把 Mosaic 的 UI 搬到写作领域，而是把它背后的“上下文驻留”思想第一次扩展到非软件开发场景。

### 8.2 从 Mosaic 借鉴什么【已定方向】

新 app 不搬 Mosaic 的 AppKit 视图，但**产品语言、交互、算法、以及 iPad 上也存在的系统能力，默认先看 Mosaic 怎么做的，再决定要不要改。** 不要因为「新 app」就把这些重想一遍。

| 借鉴 | Mosaic 里是什么 | 到写作现场 |
| --- | --- | --- |
| **连线的形态与手感** | circuit 圆角正交、1.5pt 屏幕空间线宽、底描边 knockout、直颈、Z/L/D 三族、同源错位、迟滞、拖线虚线预览（`D3 连接线.md`） | **手感可借，线形未锁。** 草图是短曲线。分叉在数据：线是带类型的持久边，而且要能命中（双向高亮），Mosaic 的线不命中、也没有 identity |
| **从把手拖出线** | 标题栏 / ＋ 手柄起手，空白处动作，已有对象吸附 | 纸的侧边起手（够手指）；松手动作见 §6.2；键盘等价必须同时存在 |
| **注意力层** | quiet / running / waiting、pill、MRU、overview；runtime only | 映射成「当前段落有几张未解决问题 / 资料 / 反馈」；仍然不把「正在请求」存进库 |
| **壳：文稿列表** | Workspace / Room 列表、当前房间一张画布、jump、chrome | **列表可借鉴，房间画布不搬。** 房间改名叫 Topic；没有磁盘 Workspace。每个 Topic 一张纸 + 四周。侧栏不是现场，不必常驻 |
| **Soundtrack / MusicKit** | 应用属性，不是 music tile；进程内 `ApplicationMusicPlayer`；chrome pill；绑定持久化、播放器状态不持久化（`Handoff · Room Soundtrack.md`） | **v1 整 app 一首**【已定方向】。Mosaic 这么做是因为房间切换很频；写作里切 Topic 稀得多，恢复现场反而可能想带上歌。v1 仍放应用设置。两周真写后再看要不要变成 Topic 绑定 |
| **视口、culling、LOD** | `CanvasTransform`、render tier、关系透明度随缩放 | **理由搬不过来。** 没有画布相机，就没有按缩放分级。在场的卡靠「这段还在不在屏幕里」切，已经是 §6.3 的 working set |
| **Jump palette / 命令目录** | 纯目录 + SF Symbol 名，不是视图 | 拉线动作、专注模式、找卡，用同一套「小目录」思路 |
| **数据层模式** | SQLiteData、UUID、JSON-in-TEXT、运行时/持久化边界 | 照搬模式；边要建表，这是明确分叉 |
| **语义命令，不模拟鼠标** | MCP 操作小、对数据 | 命令集照做；iPad 上先不接外部 agent 传输 |

**不借鉴（实现或能力在 iPad 上不成立）：** AppKit 画布与全部 ViewController、PTY / 本地 shell、ScreenCaptureKit 窗口流、Sparkle、Unix MCP socket、把线当成 membership 派生。

### 8.3 必须新做（而且这才是工程工作量的大头）

> **一个能撑住长文写作的编辑器。**

排版、光标、选区、撤销栈、中文输入法、大文档性能 —— Mosaic 现有 tile 装的是终端、浏览器、占位视图，**没有长文编辑的积累**。

⚠️ 风险提示：最初的表述是“为 markdown 写作加 AI 功能”，但真实工程工作量的大头很可能在编辑器上。低估它 = 超期。

**内核【已定方向，2026-09-17 复评】：CodeMirror 6（MIT）打进包，`WKWebView` 加载。** 取证 `research/editor-options.md`。不选 Milkdown / TipTap / MarkupEditor（contentEditable + WYSIWYG）；不选 Runestone 当第一刀（Mac 没做完）。中文 IME 是 S2，不是现在换内核的理由。工程结构给纸预留 WKWebView 即可。

### 8.4 三条实现路线【已定 = A】

| 路线 | 优点 | 缺点 |
| --- | --- | --- |
| A. 新 app，复用 Mosaic 里可移植的算法与数据层模式 | 边界干净；iPadOS + macOS 可行 | 现场要新做（Mosaic 的画布是纯 AppKit） |
| B. 完全独立实现，连 Mosaic 的模式也不借 | 无牵扯 | 把已经想清楚的持久化边界和连线问题再想一遍 |
| C. 先做成 Mosaic 的一个房间类型 | 在「同栈、AppKit 画布可复用」时最快 | 受众不对；**在「不用 AppKit、目标含 iPadOS」之后不可行** |

**当前决定**【已定，2026-09-16】：走 A。产品文档原先倾向 C，是因为当时默认和 Mosaic 同栈。平台改成 iPadOS + macOS、我们不写 AppKit 之后，C 不再可行（MosaicPackage 零 SwiftUI、画布是 `InfiniteCanvasNSView`）。

共享上下文的落点不是「Mosaic.app 里再挂一个 target」，而是**一个 Xcode workspace 里并列两个 app project**（Mosaic 只 macOS，write-field iPadOS + macOS），可移植算法走双方都链的 SPM product。见 `技术规划 v0.1 · 底座与架构.md` §2.3。

工程细节见同文档：SwiftUI 双平台，编辑器 CodeMirror 6 in WKWebView。现场是正中一张纸、周围是临时内容。文稿列表可以像 Mosaic 侧栏；写作表面不要画成 Mosaic 房间，也不要画成 IDE 分栏。

---

## 九、验证方案【待打磨】

> **“两周”是观察窗口，不是开发工期。** 最小原型做到足以承载真实写作后，再开始连续两周的使用观察。前面的探索实现可以慢，因为它本身就是产品研究。

### 9.1 当前要验证的核心假设

> 当资料、AI 产出和未完成思路能够驻留在正文周围时，作者是否会更少离开当前写作现场，并更容易恢复“我刚才在想什么”？

拉线只是其中一种交互，不再把“是否频繁拉线”当成产品成立与否的唯一代理指标。

### 9.2 原型准备阶段（不设硬工期）

只实现足以验证假设的最小闭环：

- 一个 Topic = 一张纸 + 四周 = 一篇真实文章（可从列表换 Topic；列表不是现场）
- 中央一个线性 markdown 编辑器（这张纸，不是普通卡）
- 周围只允许三种临时对象：**资料卡、问题卡、碎片卡**
- 只做一个 AI 动作：**就当前段落提问 → 回答卡驻留在附近**
- 至少支持一种手动把资料放到当前段落附近的方式
- 能保存并恢复正文位置 + 周围卡片状态
- 其余全部砍掉：找资料自动化、拆段、合并、复杂引用、批注模式、Zotero、100+ 卡片治理……

### 9.3 连续两周真实使用

- 只用真实写作任务，不能专门为了测试产品而写
- 第一个任务跑通后，不因为“想到新功能”立即加功能
- 每天只记录几个非常具体的行为和摩擦
- 第二个任务继续使用同一套原型，观察问题是否重复出现

### 9.4 观察指标

1. **离开写作现场的次数**：为了找资料、找 AI 回答、找旧文章而切去别的 app / tab 的频率。
2. **上下文丢失事件**：出现“那个东西在哪”“我刚才为什么查这个”“我上次写到这里在想什么”的次数。
3. **恢复成本**：隔几个小时或第二天重新打开文章后，到重新进入有效写作状态需要多久。
4. **卡片复用率**：一张资料 / 回答卡是否会在创建之后再次被看见、引用或影响正文。
5. **卡片 → 正文的转化率**：太低可能说明卡片没有价值；接近 100% 又可能说明卡片只是多了一道搬运手续。
6. **周围逃避行为**：是否开始主动清掉周围、把东西重新丢回浏览器 / ChatGPT，因为那样更快。
7. **成文后的主观判断**：有没有至少一次明确感觉“如果没有保留下来的写作现场，我会花更多时间重新找、重新想”。

### 9.5 判据【暂定】

- **继续**：在多个真实任务里反复出现“上下文驻留节省了重新寻找 / 重建思路的成本”，并且没有明显破坏专注。
- **降级**：四周的卡确实有价值，但「这段旁边」用不上、全局列表更快 → 再退化成 sidebar / keyboard-first 的 working set。不要回头去做无限画布。
- **停止**：用户（首先就是我们自己）持续绕开周围，或维护卡片的成本明显高于找回上下文的成本。

### 9.6 编辑器最小子集（会不会把研究拖死的关键）

必须：打字、选区、撤销/重做、中文输入法、标题、粗体/斜体、列表、footnote、链接、文件切换。

可后置：实时预览分栏、表格、图片粘贴、插件、导出。

---

## 十、待打磨的问题清单

### 现在值得回答（会直接改变下一步原型）

1. **Working Set 怎么形成？** 正在写的这段，周围已经在（§3.3–3.4）。还没验证这样会不会吵。
2. 线到底是“动作”还是“关系”？纸边到卡是动作（视图）；卡→卡派生是关系（入库）。还要看真实写作里派生边会不会被再看一眼。
3. **恢复现场要恢复什么？** 光标位置、当时在周围的卡、未解决问题、最近参照资料，哪些是真正有帮助的？（没有画布缩放。）
4. 一篇文章 = 一张纸（一个 Topic）已经是默认。长文要不要按章节再切，真实写长了再看。
5. 正文就是那张纸。不要再问「是不是一个 tile」—— tile 是 Mosaic 的词。引言 / 附录若出现，也是这篇里的结构，不是旁边另一张纸。
6. 专注模式下相关上下文以什么形式存在：默认是只剩纸、侧边端口还在。是不是就是「强制收起周围」？
7. 用什么机制保证“卡片最终回到正文”，而不是让收纳取代写作？
8. 拉线的**键盘等价**具体是什么键位？
9. 是否与 Mosaic 共享代码库，真正的分叉点画在哪？

### 暂缓回答（真实使用出现之前不设计）

10. 卡片四状态是否够用、是否要“过期”。
11. 100+ 张卡时怎样分组、折叠、缩放。
12. Zotero / 浏览器剪藏做多深，是只读导入还是双向写入。
13. 多人协作是否需要提前留数据模型。
14. 是否需要按资料 / 章节 / 状态保存多套“周围的视图”。
15. 目标人群最终是写作者、研究者、技术写作者还是学生。
16. 对外名字仍【待定】。工作名 write-field / 写作现场。**写作画布已弃用**——形态不是画布。

这些问题不是不重要，而是**现在没有足够行为证据，讨论越久越容易把想象当需求。**

---

## 十一、风险与反方论证

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| **收纳取代写作** | 笔记 / 白板类产品最常见的陷阱：整理产生即时满足，正文反而不动 | 每个对象都要有“回到正文”的路径；观察卡片是否长期只被摆放 |
| **注意力分裂** | 视野里二十张卡，与写作需要的专注直接冲突 | Working Set + 注意力层 + 专注模式；默认只强调当前真正相关的少数对象 |
| **维护周围比找东西还累** | 如果用户要手动整理很多卡片，context switch 只是从系统 tab 变成产品内部 housekeeping | 自动保留位置和关系，但尽量不要求“整理”；周围首先是驻留，不是分类 |
| **周围退化成装饰** | 大多数任务最后只需要正文 + 旁边几个相关对象 | 这正是默认形态，不是失败。若连「这段旁边」都用不上，再降成全局侧栏；不要为了显得像 Mosaic 去加平移缩放 |
| **AI 产出堆积** | AI 太容易产生大量回答，反过来污染现场 | AI 默认安静；一次动作只产生有限对象；未被再次使用的内容应自然淡出 |
| **编辑器被低估** | 工程量大头可能在光标、IME、撤销、大文档性能 | 研究阶段只做最小编辑器；不要用完整编辑器作为验证前提 |
| **探索型开发速度慢** | 很多关键交互没有现成答案，需要自己写、自己试、自己否定 | 把速度慢视为研究成本；每次只验证一个假设，不用功能数量衡量进度 |
| **既有强者** | **Pages**（白纸 + Writing Tools 改正文）、Scrivener（长文）、Heptabase（研究）、Obsidian Canvas（免费）、Milanote | 相对 Pages：形态可以像纸，AI 动词必须相反（读、不停在正文里）。相对画布类：差异点仍是现场驻留 + 减速带，不是「也有二维」 |
| **分发与变现难** | 写作工具没有天然病毒循环 | 在产品成立后再决定人群；不要现在为了市场叙事扭曲核心交互 |
| **AI 变强会让它贬值？** | 如果价值来自“AI 当主人替你写”，会；如果来自助手先放旁边、作者决定进不进纸，则未必 | 坚持助手不是主人；插入永远是作者手势 |
| **做成更差的 Pages** | 白纸形态一旦成立，最顺的下一步是接 Writing Tools、合写、版式 | 不接 Rewrite / Compose；不做人与人合写。轻量 = 不做 Pages 已经做的，不是少做自己的核心 |

---

## 十二、开发原则【已定】

这个项目的开发速度**可能会显著低于 Mosaic，而且这是可以接受的**。

Mosaic 的很多核心交互有明确的现实类比：把原本分散的工作上下文铺在二维空间里，价值可以很快通过日常开发验证。写作现场不同：我们正在试图找出“思考上下文怎样驻留、什么时候出现、什么时候退场、怎样重新收敛到正文”这一整套交互语言。很多答案只能在自己真正写东西时出现。

因此不把这个项目当成常规 feature roadmap，而当成一系列**产品实验**。

### 12.1 基本循环

> **提出假设 → 做最小实现 → 用真实任务写 → 记录行为与摩擦 → 再决定是否固化。**

不是：

> 想完整功能 → 做完整架构 → 一次性实现 → 发布后再看有没有人用。

### 12.2 具体规则

1. **一次只验证一个不确定性。** 如果不知道“回答卡放在段落旁边有没有价值”，就不要同时做十种 AI 动作。
2. **真实使用优先于讨论。** 一个交互自己连续几次没有自然使用，比十页产品论证更有信息量。
3. **允许推翻【已定】。** 【已定】表示当前判断足够强，可以进入实现；不表示永远不可撤销。真实行为证据优先级更高。
4. **不为未来规模提前设计。** 没有出现 100 张卡，就先不解决 100 张卡；没有 Zotero 需求，就先不做 Zotero；没有多人，就不为多人牺牲当前模型。
5. **工程实现是探针，不是承诺。** 原型代码可以脏、可以被删；它首先用来回答产品问题。
6. **功能速度不是进度。** 真正的进度是“不确定性减少了多少”。
7. **每次添加能力前先问：它减少了哪一种 context switch？** 如果答案只是“更强 / 更完整 / 别人都有”，默认不做。
8. **始终保留退化路径。** 默认已经不是无限画布。若周围也证明多余，再退化成 sidebar 或键盘命令；不维护形式上的原创性。

### 12.3 记录方式

每次真实写作后只记录几件事：

- 我在哪一刻离开了正文？为什么？
- 哪个上下文如果一直在旁边，会让我少切一次？
- 哪张卡我创建后再也没有看？
- 哪张卡让我第二天快速恢复了思路？
- 哪个交互让我产生了“为了使用产品而使用产品”的感觉？

这些记录比 feature backlog 更重要，它们会决定下一轮原型。

---

## 十三、下一步【已定方向，2026-09-17】

**形态够了，不要再开一轮形态探索；也不要现在进入抛光设计图。**

§3.5 那一圈已经画在 `Design/一次写作.html`。还没钉死的（左右分工、竖屏、线形、玻璃材质）是细节，画更多板也杀不死产品。能杀死产品的是 S1 / S2 / 两周真写。

| 现在做 | 现在不要做 |
| --- | --- |
| 工程结构（S0）在**另一个 session** | 再问「要不要画布 / 要不要分栏 / 要不要横向翻页」 |
| S1 按 §7.1 验收：两侧能点的卡，跟段落 Y | 七张抛光板、Liquid Glass、日历式叠卡 |
| iPad 横真实比例结构草图：`Design/iPad横-写作现场.html` | 竖屏 / 壳 / pill 的视觉定稿 |

先做一个**写作现场原型**：

- 一个真实 markdown 正文
- 一种资料卡
- 一种问题 / 回答卡
- 从当前段落发起一次 AI 提问
- AI 回答留在该段附近
- 关闭并重新打开后，能恢复上一次的正文位置和周边 working set

第一轮不要做平移 / 缩放 / 世界相机。现场就是正中一张纸。只回答两个问题：

1. **把资料和 AI 产出留在正文周围，是否真的减少了上下文切换？**
2. **隔一段时间回来时，保留下来的写作现场是否让我们更快恢复思路？**

如果这两个问题没有明显的“是”，后面的复杂连线、批注、章节空间化都没有必要继续。

> 记录原则：任何决定都写回本文件，并标注【已定】/【待定】；任何真实使用推翻了旧判断，也同样写回。

