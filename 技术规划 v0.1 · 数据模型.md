# write-field · 技术规划 v0.1 · 数据模型

> 本文只回答一个问题：**什么东西存在哪里。**
> 沿用产品文档的标注约定：`【已定】` 可以进入实现，`【待定】` 是还没钉死的。
>
> 上游约束（本轮已定）：
> 1. 平台 = iPadOS + macOS，App Store 沙盒分发；
> 2. 不搬 Mosaic 的画布实现，只借它的数据层模式与纯算法；
> 3. 正文住在应用容器里，靠导出/分享出门；
> 4. 编辑器内核用现成开源产品（见《底座与架构》），需要 LaTeX / 科学内容；
> 5. 同步走 SQLiteData `SyncEngine`（原型不开，表按它的规矩建）；
> 6. 产品单位是 Topic（= Mosaic 的 Room）：一张画布、一份主正文。表名 `topics`。

---

## 一、事实来源：四层，不是一个

这是整个数据模型最重要的一句话：

| 层 | 事实来源 | 存在哪 | 丢了会怎样 |
| --- | --- | --- | --- |
| **正文文本** | `topics.body`（纯 markdown 文本） | SQLite | 这个 Topic 的稿没了。**这是唯一的不可重建层** |
| **语义结构** | 卡片、边、锚点、布局 | SQLite 同一库 | 卡片和它的来历没了，但文章还在 |
| **写作现场** | 光标、展开的卡、未解决问题 | SQLite | 每次重开都要重新找位置 |
| **注意力瞬态** | 请求进行中、hover、选区、working set、undo 栈 | 内存 | 什么都不该发生 |

上一版写过「正文文本永不进 SQLite」。那条在「单设备、文件当源」时成立；一旦要跨设备，**.md 文件和 SQLite 是两个同步通道，不能原子对齐**（iCloud Drive 同步文件、CloudKit 同步库，谁先到谁覆盖，冲突模型直接裂开）。

本轮改成：

> **能同步的东西必须在同一张 SQLite 里。** 正文是 `topics` 表的一列。`.md` 仍然存在，但是**本地镜像 / 导出物**，随时可以从这一列重建。

FTS5 索引（§七）仍然是可重建缓存，必须随时可以删掉重建而不损失信息。它**不进** CloudKit。

## 二、文件层（导出与本地镜像）

### 2.1 一个 Topic = 一行 `topics` + 一份可重建的 `.md` 镜像【已定】

```
<container>/
  Library/Application Support/write-field.sqlite   ← 事实来源
  Documents/exports/<topicID>.md                 ← 分享/导出时生成
  Documents/mirrors/<topicID>.md                 ← 保存后的本地镜像（可选，可删）
  Documents/mirrors/<topicID>.assets/            ← 该 Topic 引用的图片等（后置）
```

- 标题是元数据不是文件名。理由：改名不能让引用断掉。
- 容器内而不是用户选的文件夹【已定】：没有外部编辑器并发写。代价是 iPad 上「我的文件」不再是一等公民 —— **导出/分享必须是第一天就有的能力**，不是后置项。
- `.md` 必须是干净的 CommonMark + 少量扩展（footnote、`$...$` 数学），**不允许写入任何应用私有的标记**（不写 block ID、不写锚点注释）。镜像和导出是给人看的，也是给别的工具读的。
- 镜像丢失 = 从 `topics.body` 再写一份。`topics.body` 丢失 = 文章没了。

### 2.2 落盘策略【已定方向，参数待定】

沙盒 + 移动端有一个 desktop 不存在的失败模式：**系统会在任意时刻回收你的进程**（内存压力、切后台、崩溃）。因此：

- **不允许「退出时保存」。** 编辑器在打字停顿后约 1–2 秒写一次 SQLite（节流 + 合并）。
- 同一拍可选地原子写镜像：写临时文件 → `rename` 覆盖。镜像失败不能回滚已经进库的正文。
- 另有一份**崩溃恢复日志**（未提交的增量或上一版全文，本地表，不同步）。启动时如果发现「日志比 `topics.body` 新」，提示恢复而不是静默丢弃。
- 输入法组合态（marked text）**不算内容**，不触发落盘。这一条是从 Mosaic 的 `NoteTextView` 继承下来的正确做法。

### 2.3 文章表【已定】

```sql
CREATE TABLE topics (
  id         TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE DEFAULT (uuid()),
  title      TEXT NOT NULL DEFAULT '',
  body       TEXT NOT NULL DEFAULT '',
  createdAt  TEXT NOT NULL,
  updatedAt  TEXT NOT NULL
) STRICT;
```

一行 `topics` = 一个 Mosaic 意义上的 Room = 一张画布。`body` 是该 Topic 的主正文，干净 markdown，和镜像文件字节级一致。CloudKit 单条记录大约 1MB 上限；长文（数十万字）要把 `body` 改成 BLOB 走 `CKAsset`。原型远低于这个阈值，先当 TEXT。

v1 不引入第二层「Workspace」表。App 直接拥有 Topic 列表。`cards` / `edges` / `anchors` / `scenes` 都挂 `topicID`。

## 三、锚点：卡片到底指着哪一段【已定方向】

### 3.1 问题

产品文档要求"每根线、每张卡都指向某一段"，但作者**一直在改字**。段落会移动、会被改写、会被删除。如果锚点是 `blockIndex = 7`，改一次就全错。

### 3.2 方案：两级锚定

**第一级 —— 记录期，用文本引用选择器（W3C Web Annotation 的 TextQuoteSelector 模型）**

```jsonc
{
  "exact":  "画布的价值是上下文驻留，而不是空间本身。",  // 选中的原句
  "prefix": "换句话说：",                                 // 前 32 字，用于消歧
  "suffix": "判断画布是否成立",                            // 后 32 字
  "hint":   {
    "blockOrdinal": 41,
    "headingPath": ["三、产品形态", "3.2 空间不是目的"],
    "docOffset": 3187,        // 源码编辑器给的文档偏移量，最精确的提示
    "line": 96, "column": 5   // 人可读的等价形式，便于调试与人工修复
  }
}
```

`docOffset` / `line` / `column` 只在**记录期**作为"上次在哪"的提示，**不参与正确性判断**——真正的等价判断永远靠 `exact + prefix + suffix`。理由：偏移量在一次编辑后就全错，而引用文本能在整篇里被重新找到。

这个设计成立的前提恰好是本轮已定的编辑范式：**源码模式**。源码编辑器里光标位置天然就是 `(offset)` / `(line, column)`，不需要从任何文档模型里反推，桥要传的只是几个整数。

**第二级 —— 解析期，把正文切成块并回填**

载入文章时把 markdown 解析成 block 列表（标题 / 段落 / 列表项 / 代码块 / 引用块），每块算出 `(ordinal, headingPath, contentHash)`；锚点用 `exact` 全文匹配 + `prefix`/`suffix` 打分消歧，先试 `hint` 附近，再试全文。

### 3.3 两条硬规则【已定】

1. **解析失败时绝不自动吸附到"最像"的段落。** 显示"锚点已失效，请重新指定"，把选择权交回作者。
2. **锚点只进库，不进 `.md`。** 文件保持纯净。

规则 1 的理由和产品文档 §二排除"内容真伪验证"是同一条：**错误归属会制造虚假安心，比明说"我不知道"更糟。** 一张"依据"卡如果悄悄挂到了错误段落上，作者会以为自己在引用某段，实际没有 —— 这正是这个产品最不能犯的错。

### 3.4 边和卡都挂在锚点上，不挂"段落对象"【已定】

卡片 / 边 → `anchorID` → 解析出的 block。段落没有自己的稳定 ID，因为文件里不允许写 ID。

**同一 block 可以有多行 `anchors`。** `exact` 不同，就是不同的句子/短语；产品文档 §3.4 规定画面上仍合成 **一个** gutter hub。不要为 hub 建表，不要把 `blockOrdinal` 提成主键——那只是解析期的 hint。双向高亮走 `exact`，working set 走「这个 block 还在不在屏幕里」。

```sql
CREATE TABLE anchors (
  id         TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE DEFAULT (uuid()),
  topicID  TEXT NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
  selector   TEXT NOT NULL DEFAULT '{}',
  createdAt  TEXT NOT NULL
) STRICT;
```

`selector` 是上面那份 TextQuoteSelector JSON。形状变了只改 JSON，不开新列。

### 3.5 谁来做 block 解析：Swift 侧，不用编辑器内核【已定】

锚点解析由 **Swift 侧自己解析 markdown**，用 [`swift-markdown`](https://swiftlang.github.io/swift-markdown/documentation/markdown/text/range/)（Apple 开源，纯 Swift + cmark-gfm，无 iOS 限制）：每个 `Markup` 节点带 `range: SourceRange?`，`SourceLocation` 给出行列，正好是锚点需要的 `blockOrdinal` 与前后文。

为什么不让编辑器内核（可能是 WKWebView 里的 JS 编辑器）告诉我们在哪：

1. **独立性。** 锚点系统是数据层的东西，不该随编辑器内核的存亡而存亡。换编辑器时锚点不能集体失效。
2. **一致性。** 卡片/边的锚点必须能在**没有打开编辑器**的时候解析（列表页、跨文视图、索引重建）。
3. **可测试。** 纯 Swift 的解析器可以进单元测试，能拿真实中文长文做回归；桥另一侧的逻辑不行。

代价：Swift 的解析结果可能与编辑器自己的 AST 有细微差别（例如编辑器对 `$...$` 数学的处理）。这属于可接受的工程折衷——**锚点只需要定位到块，不需要理解块的语义**。

## 四、卡片

```sql
CREATE TABLE cards (
  id           TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE DEFAULT (uuid()),
  topicID      TEXT NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
  kindRaw      TEXT NOT NULL,   -- clip | question | answer | snippet | section | annotation | discarded
  title        TEXT NOT NULL DEFAULT '',
  body         TEXT NOT NULL DEFAULT '',
  stateRaw     TEXT NOT NULL,   -- draft | ready | used | discarded
  provenance   TEXT NOT NULL DEFAULT '{}',  -- JSON，形状随 kind 变，见第五节
  parentCardID TEXT REFERENCES cards(id) ON DELETE CASCADE,  -- answer → question
  anchorID     TEXT REFERENCES anchors(id) ON DELETE SET NULL,
  x REAL, y REAL,               -- 画布坐标
  createdAt    TEXT NOT NULL,
  updatedAt    TEXT NOT NULL
) STRICT;
```

- **状态只有四态**（草稿 / 待用 / 已用 / 废弃），沿用产品文档 §二。
- **回答卡必须携带它回答的那个问题**（`parentCardID`）。产品文档反复强调"关系不按时间发生"，如果回答卡脱离问题单独漂在画布上，它就退化成聊天记录。一问一答用这列；一张新卡来自**多张**资料，走 `edges`（`derived`），不要把 `parentCardID` 加成多父。
- **Hub → 卡不是 `edges` 行。** 那段有哪些卡，解析 `anchorID` 就能得到。入库的是卡与卡、卡与「引用了哪句」的类型边。
- **`x/y` 允许为 NULL**，表示"还没被作者安置"。自动布局只负责给 NULL 的卡一个位置，**不负责重排已经手动放好的卡**。
- 沿用 Mosaic 的做法：**JSON 塞进现成的 TEXT 列**，形状变化不开新迁移。

## 五、来源（provenance）：让"编造引用"在类型上不可能

产品文档 §七 把"编造引用"列为**架构级禁止**。这条不能靠提示词，要靠类型。`provenance` 是一个判别联合，**只有三种合法形态**：

| 形态 | 何时产生 | 必填字段 |
| --- | --- | --- |
| `clipped` | 作者亲手剪藏 | `url`、`pageTitle`、`selectedText`（剪藏当时抓的原句）、`clippedAt` |
| `retrieved` | 应用真的发起了检索并拿到了结果 | `query`、`resultURL`、`resultTitle`、`snippet`、`retrievedAt`、`engine` |
| `asserted` | 模型自己说的，没有检索来源 | `model`、`modelVersion`、`askedAt` |

**没有第四种形态。** 因此不存在"编造出处的引用"这个对象，因为没有任何构造器能生成它。

配套的三条规则：

1. `asserted` 卡**可以存在**，但 UI 必须显示为"无来源"，并且**不允许被拖进正文变成 footnote / 引用**。它可以当提问的引子，不能当依据。
2. 只有 `clipped` / `retrieved` 能成为"引用"边或 footnote 的目标，且创建时必须**当场验证 URL 可解析**（一次网络请求）。验证失败的卡降级成 `asserted` 并保留原因。
3. 检索结果**原样保存**拿到的那段文本。不要保存"模型对检索结果的转述" —— 那正是引用失真的入口。

## 六、边

```sql
CREATE TABLE edges (
  id         TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE DEFAULT (uuid()),
  topicID  TEXT NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
  typeRaw    TEXT NOT NULL,   -- reference | basis | rebuttal | open | derived
  -- derived: card → card（这张是从那张长出来的）。不要用来表示「卡吸收进正文」
  sourceKind TEXT NOT NULL, sourceID TEXT NOT NULL,   -- card | block
  targetKind TEXT NOT NULL, targetID TEXT NOT NULL,
  note       TEXT NOT NULL DEFAULT '',
  createdAt  TEXT NOT NULL
) STRICT;

CREATE INDEX edges_by_target ON edges(topicID, targetKind, targetID);
CREATE INDEX edges_by_source ON edges(topicID, sourceKind, sourceID);
```

### 6.1 这是与 Mosaic 的明确分叉【已定】

Mosaic 的关系模型是**派生的**：`ConnectionID(workspaceID, nodeID)` 由 tile 的 membership 推出来，**库里没有 wire 行**（见 `Mosaic · Workspace Hub v3 · D3 连接线.md` 的对照表与 `Mosaic · Architecture & Data Model.md`）。

写作画布不能这么做。产品文档 §5.2 要的是相反的东西：**每根线一画出来就持久存在、并且带类型**，因为

- 它是动作的**痕迹**（三个月后知道这里为什么有张卡）；
- 它是结构的**来源**（边一多，结构自己浮现）；
- 它是**双向高亮**的基础。

这三条都要求边是独立于任何 membership 的一等实体。所以：**必须建表，必须带类型，删卡不删边的语义要显式定义**（删卡时边级联删除，但"废弃卡"保留边）。

### 6.2 跨文章的边不存在【已定】

产品文档 §七 的"跨文重复 / 矛盾检测"产出的是**引用列表**，是个派生视图，不是存储的边。不要在第一天为跨文关系建模型。

## 七、索引（可重建缓存）

- FTS5 覆盖文章的 block + 卡片正文，用于 §七 的"**先搜我自己的语料**"。
- **它是缓存，不是事实来源**：删掉必须能全量重建。
- 【待定】向量索引何时引入。跨文重复/矛盾检测是长期护城河，但它不是原型要验证的假设，先不做。

## 八、写作现场（scene）

```sql
CREATE TABLE scenes (
  topicID        TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE
                   REFERENCES topics(id) ON DELETE CASCADE,
  caretAnchor      TEXT NOT NULL DEFAULT '{}',  -- 与卡片同构的锚点 JSON
  viewportX        REAL NOT NULL DEFAULT 0,
  viewportY        REAL NOT NULL DEFAULT 0,
  viewportZoom     REAL NOT NULL DEFAULT 1,
  openCardIDs      TEXT NOT NULL DEFAULT '[]',
  unresolvedCardIDs TEXT NOT NULL DEFAULT '[]',
  lastOpenedAt     TEXT NOT NULL
) STRICT;
```

### 8.1 这里和 Mosaic 的规矩是冲突的，要说清楚

常被误读的一条：Mosaic 的"关注状态只在运行时"。它真正禁的是两件不同的事，必须分开看：

**（1）它禁"恢复后会谎报当前状态"的东西。**
`Mosaic · Handoff · Attention Layer.md` 的 Do not 写得很直白：不持久化 attention / MRU / overview，理由是代码注释里那句话 —— *"a 'waiting' that survives launch is a lie"*（重启后还在的"等待中"是撒谎）。

**（2）它禁"高频瞬态"。** `Mosaic · Architecture & Data Model.md` 的"不应保存"一栏列的是：**高频**光标位置、拖动中的**每一帧** viewport、实时画面帧、瞬态 UI 状态。

**它并不禁"已保存的现场"**——同一份文档的"建议保存"一栏里明确有**"房间的已保存 `Viewport`"**。所以把"上一次离开时的视口"存下来，**不是分叉，是 Mosaic 已有的做法**。

于是判据可以收得非常紧：

> **恢复它，会不会让作者以为某件事"现在仍然成立"？**
> 会 → 运行时。不会，且它是作者主动留下的、稳定下来的现场 → 入表。

| 状态 | 判定 | 去向 |
| --- | --- | --- |
| 光标位置、视口与缩放 | 稳定下来的现场 | **入表**（Mosaic 本来就存 viewport） |
| 展开的卡、未解决问题 | 作者主动留下的上下文 | **入表**（这是本项目的新东西） |
| 后台请求进行中、流式回答 | 恢复它会假装还在跑 | 运行时 |
| hover / 选区 / 双向高亮 | 纯瞬态 | 运行时 |
| working set 的计算结果 | 可随时重算 | 运行时 |
| 拖动中的每一帧坐标 | Mosaic 明确禁止的高频写入 | 运行时 |
| undo / redo 栈 | 跨启动保留会让人以为能撤销昨天 | 运行时 |
| 「这里有东西在等你」的小点 | 恢复陈旧提醒 = 制造虚假紧迫 | 运行时 |
| **Soundtrack 绑定** | 整 app 一首，不是某篇文章的现场 | **应用设置表**，不进 `scenes`。换文章、恢复现场都不改歌 |

结论：**真正的分叉只有一行**（"展开的卡 + 未解决问题"，即写作现场本身），其余都在 Mosaic 已有的边界之内。这比初看时小得多。

### 8.2 【待定】视口要不要恢复

一篇文章通常分很多次写。恢复到"上次的视口"可能不如"回到最后编辑的那一段"——因为上次你可能是在翻找资料，不是在写。**原型先只恢复光标与其周边，视口作为待验证项**，由产品文档 §九 的真实使用观察来决定。

## 九、和 Mosaic 持久化约定的对照

| Mosaic 的约定 | write-field | 是否分叉 |
| --- | --- | --- |
| 单一 UUID 主键，避免复合主键 | 同 | 否 |
| JSON 塞进现成 `content`/TEXT 列，不为小改动开迁移 | 同（`provenance`、锚点、ID 列表） | 否 |
| 运行时句柄 / 位图 / 高频帧不落库 | 同（见 8.1） | 否 |
| 关注状态一律不落库 | 同；真正的分叉只有"写作现场本身" | **部分是**，见 8.1 |
| 已保存的 `Viewport` 落库 | 同 | 否 |
| 高频光标 / 每帧 viewport 不落库 | 同 | 否 |
| 关系由 membership 派生，无 edge 行 | **edges 建表，带类型** | **是**，见 6.1 |
| STRICT 表 + 命名迁移 + 小步迁移链 | 同 | 否 |
| 为节点类型建可查询字段，按容器分批加载 | 同（`topicID` 从第一天就进每一张表） | 否 |

`topicID` 必须从第一天就出现在每一张表上。理由：跨文（多篇文章同时开着）迟早要来，而事后往每张表补作用域是所有重构里最容易出错的一种。

## 十、这个模型明确不解决的问题

- 不做多人协作，所以不为"两个作者同时改一段"留任何字段。
- 不做卡片状态过期、不做 100+ 卡的分组折叠。
- 不做 Zotero / 双向写入外部文献库。
- 不判断内容真伪 —— 库里没有任何"真 / 假 / 已验证"字段，只有"从哪来"。

---

## 十一、存储引擎【已定】

沿用 Mosaic 那一套：**SQLiteData + StructuredQueries（pointfreeco）**，直接建 SQLite。

取证（本机依赖清单，非网传）：

| 依赖 | 版本 | 自己声明的平台 |
| --- | --- | --- |
| `sqlite-data` | 1.9.0 | `.iOS(.v16)` / `.macOS(.v13)` |
| `swift-structured-queries` | 0.35.0 | `.iOS(.v16)` / `.macOS(.v13)` |
| `swift-composable-architecture` | 1.26.1 | `.iOS(.v16)` / `.macOS(.v13)` |

证据：`/Users/anderson/Developer/Mosaic/MosaicPackage/.build/checkouts/*/Package.swift`。

因此：**数据层与模型层可以跨平台复用，不需要为 iPadOS 换存储方案。** 这也是路线 A（复用 MosaicPackage 的可移植部分）最实在的一块收益。

选 SQLite 而不是 SwiftData 的理由：Mosaic 的迁移链 / STRICT 表 / JSON-in-TEXT 模式已经验证过，而这些正是本文档里最需要精确控制的部分（锚点、边、现场恢复都不是"对象图"，是关系数据）。SwiftData 的关系表达与迁移控制在这一点上更弱。

新 app **不把 TCA 当起步依赖**（见《底座与架构》§二）。上表里的 TCA 只说明 Mosaic 那套栈能编 iOS，不是 write-field 要把它整个搬过来。

---

## 十二、同步策略【已定方向】

原型：**单设备，不创建 `SyncEngine`，不申请 iCloud entitlement。** 验证期只用一台设备写（产品文档 §9.3），这同时是存储层的安全边界。

后续同步：**同一套表，打开 SQLiteData `SyncEngine`。** 不自研同步，不把 `.md` 放进 iCloud Drive 当第二通道。

### 12.1 第一天就遵守的 schema 规矩

来自本机 `sqlite-data` 的 `CloudKitSync.md`，不是网传：

| 规矩 | 我们怎么做 |
| --- | --- |
| 主键必须是全局唯一 UUID，不能自增 | 所有表 `TEXT PRIMARY KEY NOT NULL ON CONFLICT REPLACE DEFAULT (uuid())`，`scenes` 除外 |
| 每张要同步的表都要有单一主键，禁止复合主键 | `scenes` 用 `topicID` 当主键（一对一，SQLiteData 允许 FK-as-PK） |
| 禁止主键以外的 `UNIQUE` | 标题、URL、锚点文本都不加 UNIQUE |
| `ON DELETE` 只允许 `CASCADE` / `SET NULL` / `SET DEFAULT` | 已按此写 |
| 后加的 `NOT NULL` 列必须带 `ON CONFLICT REPLACE DEFAULT` | 迁移时遵守 |
| 禁止删列、改名列、改名表 | 废弃用应用层状态，不加 DROP |

`SyncEngine` 要显式列出同步哪些表。不同步的放进 `privateTables`：

- **同步：** `topics`、`anchors`、`cards`、`edges`、`scenes`
- **不同步：** FTS5 虚表、崩溃恢复日志、任何缓存

### 12.2 冲突：按列 last-write-wins【必须看清】

SQLiteData 当前**只有**这一种策略，不可定制，不是 CRDT。两台设备改同一行的同一列，后写的赢。

| 对象 | LWW 可不可接受 |
| --- | --- |
| 卡片位置、状态、边的增删 | 可接受。丢一次摆放，不丢文章 |
| 卡片正文、来源 JSON | 勉强。两台同时改同一张卡很少见 |
| **`topics.body`** | **危险。** 一整列正文是一个字段。两台同时改同一篇，后保存的那一版整篇覆盖，中间句子可能消失 |

所以：

1. 验证期仍然单设备。打开 SyncEngine 之前，产品上必须有「另一台设备刚改过这篇」的可见提示，不能静默覆盖。
2. 【待定】打开同步时正文是继续当 TEXT 列（简单、有覆盖风险），还是大到阈值改 BLOB/`CKAsset`（仍然是整篇 LWW，只是能更大）。**不要幻想 SQLiteData 会做段落级 merge。**
3. 账号切换时 SyncEngine 默认会清本地库。写作 app 必须 override `SyncEngineDelegate.accountChanged`，先问再清。

### 12.3 和「磁盘上的真实 markdown」怎么共存

产品要的是「正文是人能读的 markdown」，不是「事实来源必须是文件系统」。工程对应：

- 对人、对外部工具：导出 / 分享 / 本地镜像，永远是干净 `.md`；
- 对同步、对恢复、对 AI：只认 `topics.body`。

---

## 附：需要验证的假设（进入实现前）

1. 【待定】锚点解析的准确率：真实中文长文里，`exact + prefix/suffix` 消歧在"常见短句"上会不会误配。需要一个离线测试集。
2. 【待定】1–2 秒节流落盘在 iPad 大文档上的功耗与延迟。
3. 【待定】如果编辑器内核是 WKWebView 里的 JS 编辑器，它自己的文档模型与本文的 block 划分是否一致；不一致时以谁为准（§3.5 的独立性设计就是为了让这个冲突不致命）。
4. 【待定】打开 CloudKit 时，`topics.body` 的整列 LWW 有没有可接受的产品包装（§12.2）。
