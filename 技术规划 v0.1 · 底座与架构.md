# write-field · 技术规划 v0.1 · 底座与架构

> 本文回答：**写在哪、画布用谁、编辑器用谁、第一刀怎么切。**
> 数据怎么存见《数据模型》；AI 能做什么见《AI 语义面》。
> 给后续 agent 的操作入口是仓库根目录的 `AGENTS.md`。
> Mosaic 一侧的契约（不要加 iOS target、workspace 形状、worktree 习惯）写在
> `/Users/anderson/Developer/Mosaic/Documentation/Mosaic · WriteField.md`。
>
> **分工：** 产品、规划、spike 顺序在本仓库讨论和改文档。WriteField 需要的
> Mosaic 工程改造（可移植 SPM、Package.swift 平台、以后的根 workspace）在
> Mosaic 仓库做，从 M0 抽 `MosaicGeometry` 开始。不要把产品构思拷进 Mosaic。
>
> 上游约束（本轮已定，来自 2026-09-16 讨论）：
> 1. 新 app，不做成 Mosaic 的一个 room；
> 2. 目标 iPadOS + macOS，App Store 沙盒；
> 3. 我们不写 AppKit 界面；
> 4. 编辑器内核用现成开源产品，源码模式，公式手写 LaTeX、靠预览；
> 5. 正文住在应用容器里，靠导出/分享出门。

---

## 一、平台路线【已定】SwiftUI 双平台，不走 Catalyst

| 路线 | 判决 |
| --- | --- |
| **SwiftUI multiplatform（iPadOS + 原生 macOS）** | **采用** |
| Mac Catalyst（一份 UIKit 跑到 Mac） | **不采用** |
| Electron / Tauri / 自带 Chromium | **排除**（iPadOS 无目标；Guideline 2.5.6 强制 WebKit） |
| 纯 Web（把整个产品做成 WKWebView 里的站点） | **排除**（Guideline 4.2 最低功能；画布 + 原生手势 + 沙盒文件会变成套壳浏览器） |

### 1.1 为什么不是 Catalyst

Catalyst 能让 `UITextView` / Runestone 在 Mac 上跑起来，但这个产品的主表面是**画布 + 指针 + 精确拖线**，不是一张 iPad 表单。

取证（`research` 里那份 Catalyst 对照，及 Apple HIG / WWDC）：

- HIG 原文：以「marking, handwriting, or navigation」为主功能的 app，**可能不适合**用 Catalyst 上 Mac。
- 画布依赖的滚动 / 捏合，在 Catalyst 上是 UIKit 的 `scroll` / `transform` 事件，不是 AppKit 的 `NSEvent.magnification`。Apple 自己还说：不要把捏合/滚动当成核心功能的唯一入口（WWDC21-10053）。
- 菜单栏只从 `UIApplication` 建，不是按窗口建。
- WWDC24/25/26 目录里没有 Catalyst 专题；未弃用，但也没有新投入的证据。

这些不是「Catalyst 做不出来」，是**把唯一一份手势实现押在一条 Apple 已经不主推的适配层上**。画布手势一旦在 Catalyst 上拧巴，没有第二条实现可退。

SwiftUI 双平台的代价是：Mac 上 SwiftUI 底下仍是 AppKit。我们接受这个系统事实，**但不在自己的代码里写 `NSView` / `NSTextView` / `InfiniteCanvasNSView`。** 第三方库内部的 `#if os(macOS)` 适配（例如 swift-flow 的 `CanvasHostView`）不算违反这条。

### 1.2 最低系统

swift-flow 声明 iOS 26 / macOS 26。第一刀就跟它走。【待定】若 spike 证明它没用到 26-only API，再考虑下调；不要第一天去跟库的 Package.swift 打架。

---

## 二、和 Mosaic 的关系【已定】新 app；借鉴能力，不搬 AppKit 视图

产品文档 §8.4 原先倾向 C（Mosaic 的一个 room）。那是在「和 Mosaic 同栈、AppKit 画布可复用」的前提下说的。平台改成 iPadOS + 不用 AppKit 之后，C 不再可行：

- MosaicPackage 205 个 Swift 文件、72,104 行；**104 个文件 `import AppKit`，0 个 `import SwiftUI`，0 个 `import UIKit`。**
- `InfiniteCanvasNSView` 2805 行，一行都带不到 iPad。
- 审计全文：`research/mosaic-reuse-audit.md`。

这只说明**视图层要新写**。Mosaic 里已经做对的交互、线形、系统能力，默认借鉴，不重做。产品文档 §8.2 是清单；下面是工程落点。

### 2.1 默认借鉴

| 能力 | 从 Mosaic 带走什么 | 怎么落地 | 第一轮 spike |
| --- | --- | --- | --- |
| **连线形态** | `WorkspaceWireRouter` + `WorkspaceCircleGeometry` + `WorkspaceHarnessGeometry`（纯 Foundation，`Double` 世界坐标）；circuit 圆角正交、1.5pt 屏幕空间、knockout、直颈 16、转角 14、族 Z/L/D、UUID 序错位、迟滞 | 不要用 swift-flow 默认的 bezier / smoothStep 当产品线形。给 `FlowCanvas` 接 `EdgePathCalculating`，输入改成我们的边，输出仍是 cubic。线宽、层级（卡之下）、拖线虚线预览跟 D3 | S1 可以先用库默认线证明平移/缩放；**S4 之前换成 circuit**，否则验证的是错的手感 |
| **拖线手势** | 明确把手起手、空白处动作、已有对象吸附、取消区、一键撤销 | 段落 gutter → 世界坐标 → `edges` 行。Mosaic 线不命中；我们的线要命中（双向高亮） | S4 |
| **注意力** | `TileAttention` / `TileFocusMRU` / `TileAttentionStore` 的语义与「runtime only」边界 | 映射到未解决问题 / 附近的卡；正在请求的点不落库 | S6 前后，不单独开 spike |
| **MusicKit / Soundtrack** | 应用属性，不是 music tile；`ApplicationMusicPlayer` 进程内播放；绑定（`kind` + catalog id）持久化，播放器状态不持久化；chrome pill，不把 Music.app 拉到前台。MusicKit / MediaPlayer **iOS 上存在**（审计已核对） | **整 app 一首**【已定】，和 Mosaic 一样：换文章不换歌，现场恢复不带回配乐。SwiftUI 重做 picker | **不进 S0–S7。** 验证「上下文驻留」不依赖配乐；S6 通了再做。pill 的位置跟界面设计走 |
| **视口 / culling / LOD** | `CanvasTransform`、render tier、关系透明度 | 卡片画布需要；正文表面锁 1x | 跟 S1 |
| **Jump palette** | 纯目录，SF Symbol 名是 `String` | 拉线动作表、命令面板 | 键盘等价用得到时再抄 |
| **数据层模式** | UUID、STRICT、JSON-in-TEXT、迁移链 | 见《数据模型》。边建表是分叉 | S0 |
| **语义命令** | 小、对数据、不模拟鼠标 | 《AI 语义面》§二；不做 MCP 传输 | S6 |

### 2.2 明确不搬

| 不搬 | 理由 |
| --- | --- |
| MosaicPackage 当 SPM 依赖 | `platforms` 只有 macOS，104 个文件 `import AppKit` |
| `InfiniteCanvasNSView` 以及全部 `*ViewController` / `MosaicComponents` | 纯 AppKit |
| 线的 **membership 派生模型** | 写作画布要持久的、带类型的、可命中的边 |
| PTY、ScreenCaptureKit、Sparkle、`Process` 调 git、Unix MCP socket | iPad 上不成立，或与沙盒/审核冲突 |
| `AppFeature` TCA 壳 | 不为没搬过来的 AppKit 壳买单。新 app 用 Observation + `@FetchAll` |

**需要算法时：抄纯 Foundation 文件，不要 `import MosaicCore`。** 第一批就是连线路由那一组。几何类型是 `CanvasPoint`/`Double`，swift-flow 是 `CGPoint`/`CGFloat` —— 在 `EdgePathCalculating` 适配层转一次，不要改 Mosaic 的算法去迁就库。抽成双方都链的 SPM product 之后，这条改成「依赖那个 product」，见 §2.3。

状态层：**不用 TCA 起步。**

### 2.3 共享上下文的单位：workspace，不是 Mosaic.app 里的一个 target【已定方向】

你要的是连线文档、MusicKit 手感、`WorkspaceWireRouter`、agent 读同一份设计语言 —— 这些是**仓库 / Xcode workspace 级**的共享。把它做成 Mosaic.app 的第二个 target 会把 iPadOS 编进一份 `deploymentTarget: macOS: "15.0"` 的工程里（`Mosaic/project.yml`），还会撞上 Mosaic 自己的禁令：`AGENTS.md` 写明 Companion parked，**不要给 Mosaic 加 iOS target**。

正确切分：

| 层 | 是什么 | 平台 |
| --- | --- | --- |
| **Workspace** | 一个永久入口 `.xcworkspace`，人与 agent 都打开它，不要再单独开 `Mosaic.xcodeproj` | 容器，无平台 |
| **Mosaic.xcodeproj** | 现在的 AppKit macOS app + Sparkle / 公证 | **只 macOS** |
| **WriteField.xcodeproj**（或同一生成工程里的第二个 app target） | 新的 SwiftUI 双平台 app，独立 bundle id、独立 scheme、独立签名 | **iPadOS + macOS** |
| **可移植 SPM product** | 先是 `MosaicCore` 里纯 Foundation 的那一部分（几何、circuit 路由、attention 词汇），必要时再加一层更小的 `MosaicWires` | **iOS + macOS** |
| **macOS-only SPM product** | `MosaicCanvas` / `Components` / `Features` / `Control` | 仍只 macOS |

WriteField **只链可移植 product**。Mosaic 继续链全部。Package.swift 今天是 `platforms: [.macOS(.v15)]` 且 Canvas 无条件 `import AppKit` —— 所以不是「加一个 iOS destination」就完了，是**先把能编 iOS 的 target 从编不了的 target 里拆开**（或至少给 Core 加上 `.iOS`，并保证 WriteField 不链 Canvas）。

两种都能用、不要混：

1. **一个 workspace，两个 xcodeproj**（你问的这种）。Mosaic 的 XcodeGen / Sparkle / Xcode Cloud 脚本不动；WriteField 自己一份 `project.yml`。共享靠双方都声明同一个 local package。注意：两边不要各 resolve 一份互相打架的 `Package.resolved`；以 workspace 为包图的根。
2. **一个 workspace，一份 XcodeGen 工程，两个 app target**（`Apps/Mosaic` macOS + `Apps/WriteField` 双平台）。入口更干净，但要改 Mosaic 现有的 `project.yml` 和 CI，研究阶段不值得先付。

当前 Mosaic 的 `Mosaic.xcodeproj/project.xcworkspace` 只是 `location = "self:"` 的隐式包装，**不是**能挂第二个 project 的产品入口。真做的时候要一个**根** workspace 文件，把两个 project 和 local package 写进去。

**现在先不搬仓库。** spike 仍在 write-field 这份目录里进行。等 S4 要稳定链电路由、或来回切仓库已经在丢上下文，再把 WriteField 工程放进 Mosaic 仓库、加上根 workspace。在那之前「共享上下文」靠：write-field 的 `AGENTS.md` 指向 Mosaic 文档，算法文件按需抄。搬进去之后怎么开发，见 §十。

---

## 三、画布：swift-flow 作为第一候选【已定方向】

用户点名 [1amageek/swift-flow](https://github.com/1amageek/swift-flow)。取证见 `research/swift-flow.md`。

### 3.1 为什么值得用

它已经把三条最贵的缝对上了：

1. SwiftUI `Canvas` 批量画边（性能）；
2. 节点是 SwiftUI view，**并且**为 `WKWebView` 做了 `LiveNode`（持久挂载 + 空闲海报 + 标题条拖拽，避免 WebView 吞手势）；
3. `FlowEdge` 是带 id 的一等对象，可序列化 —— 和 Mosaic 的派生线正好相反，和产品文档 §5.2 正好相同。

许可 MIT，iPad / Mac 双平台，库内部的 AppKit 只用于 Mac 上的滚动、放大、光标。

### 3.2 正文不是普通节点【已定方向】

产品形态是「无限画布 + **中央一个线性正文**」，不是「编辑器也是画布上的一张 tile」。

swift-flow 的默认世界里，所有节点共享缩放。把长文源码编辑器放进会缩放的 `FlowNode`，写字会不可用。专注模式（产品文档 §6.3，生存条件）也会变得很别扭。

因此分层是：

```
SwiftUI 窗口
├── FlowCanvas          ← 卡片、边、视口；可缩放
└── 中央正文表面         ← 不随画布缩放；源码编辑器住在这里
        └── 段落 gutter ──坐标转换──► FlowStore connection draft
```

产品方向（见产品文档 §3.3）：gutter 上的锚点是 hub（跟正文表面走，不随画布缩放）；展开的卡才是 `FlowNode`。circuit 线是这两层之间的坐标转换，不是画在编辑器里的装饰。

卡片 = `FlowNode`。边的**类型与归属**只存在我们的 SQLite；**几何**投影成 `FlowEdge`，路径用 Mosaic 的 circuit 路由，不用库自带的 bezier。`FlowStore` 是运行时投影，不是事实来源 —— 库再改 API，文章不能丢。

【待定】中央表面的具体形态（固定 overlay / 分栏 / 仅在「写作态」把相机锁到 1:1）。spike 里用 overlay 起步，因为它最不容易把编辑器卷进缩放；hub 挂在段落 Y 上，也更吃 overlay，不吃「正文也是会缩放的节点」。

### 3.3 必须避开的库陷阱

- Mac 默认 LiveNode 海报走 **ScreenCaptureKit**。写作 app 不能要「屏幕录制」。正文和任何 Web 卡都要注册 `WKWebView.takeSnapshot`。
- 单维护者、0.21。所以「可替换」是架构要求，不是口号：SQLite 是源，画布是视图。

### 3.4 退路

若 spike 证明 swift-flow 的手势和中央编辑器抢事件抢到不可用：

1. 丢掉 FlowCanvas，自己用 SwiftUI `Canvas` 画边、用 `ScrollView`/`MagnifyGesture` 做视口（丢掉的是 12k 行库，不是数据模型）；
2. 画布降级成 sidebar + working set（产品文档 §9.5 已允许这条退化）。

不要退回 Mosaic 的 `InfiniteCanvasNSView`。

---

## 四、编辑器：CodeMirror 6 in WKWebView【已定方向】

取证见 `research/editor-options.md` 与 `research/apple-swiftui-math.md`。

| 项 | 决定 |
| --- | --- |
| 范式 | 源码模式 + 语法高亮。公式在源码里就是 `$...$` |
| 内核 | 包内 CodeMirror 6（MIT），`WKWebView` 加载 |
| 预览 | 包内 KaTeX；独立表面，不在源码流里即时渲染。原型可后置 |
| 桥 | `WKScriptMessageHandler`：文本、选区、`docOffset`、composition 标志、gutter 起手 |
| 落盘 | composition 期间不写库（继承 Mosaic `hasMarkedText` 那条正确约束） |
| 锚点 | Swift 侧 `swift-markdown` 解析，不信编辑器 AST（见数据模型 §3.5） |

不选 Runestone：它是更好的**原生 iOS** 源码编辑器，但 Mac 主线没做完，Catalyst 作者自己说不算完成。要同时满足「原生 Mac」和「不用 AppKit」，它没有路。

不选 TextKit 2 自研：产品文档 §8.3 已经把编辑器标成工作量最大头；本轮明确「选开源产品」，就是为了不在这里把自己做死。

### 4.1 App Store

包内 JS 合法（2.5.2 禁的是下载代码）。用自定义 scheme 加载，不走 `file://`。不申请 JIT / alternative browser engine。

### 4.2 失败姿态

中文 IME 在 WebKit + CodeMirror 上是**已知高风险**，文档消不掉，只能 spike（见第七节 S2）。若 iPad 上 composition 不可用，退路是：

- iPad 换 Runestone（UIKit），Mac 仍用 CodeMirror；锚点系统不受影响，因为解析在 Swift 侧；
- 这会引入两套内核，只在 IME 证明失败之后才值得付。

---

## 五、分层

```
┌─────────────────────────────────────────────┐
│ SwiftUI 壳（窗口、菜单、专注模式、导出）        │
├─────────────────┬───────────────────────────┤
│ 中央正文表面     │ FlowCanvas（卡片 + 边投影） │
│ WKWebView+CM6   │ LiveNode 只给需要原生视图的卡│
├─────────────────┴───────────────────────────┤
│ 语义命令层（《AI 语义面》§二）                 │
│ 没有 body.write。没有定时器。                  │
├─────────────────────────────────────────────┤
│ 领域层：Article / Anchor / Card / Edge / Scene│
│ 锚点解析（swift-markdown）                    │
├─────────────────────────────────────────────┤
│ SQLiteData + StructuredQueries               │
│ topics / anchors / cards / edges / scenes    │
│ FTS5 本地可重建，不进 CloudKit                 │
└─────────────────────────────────────────────┘
```

规则：

1. **UI 可以脏，表不能脏。** 原型画布可以换，行的形状按《数据模型》来。
2. **AI 只通过语义命令层写卡和边。** 命令层下面没有「把这段改了」的函数。
3. **坐标转换是显式的。** 编辑器 gutter 的屏幕点 → `Viewport.screenToCanvas` → 世界点。不要让 WebView 知道画布，也不要让画布解析 markdown。

---

## 六、同步：第一天把表准备好，不打开 SyncEngine【已定】

你的判断成立：SQLiteData 的 `SyncEngine` 就是这条产品该用的同步，不必自研，也不该把 `.md` 和 SQLite 分成两个同步通道。

细节写在《数据模型》§十二。这里只钉架构后果：

- 原型 = 单设备，零 CloudKit 代码，零 iCloud entitlement。
- 表从第一天就按 SyncEngine 的规矩建（UUID 主键、`NOT NULL ON CONFLICT REPLACE`、无额外 UNIQUE、无复合主键）。
- FTS5、崩溃恢复日志 = `privateTables`，永远不同步。
- 打开同步之前必须接受一件事：正文是一整列 TEXT，冲突策略是**按列 last-write-wins**。两台设备同时改同一 Topic，后写的那一列赢，中间句子可能消失。所以验证期仍然「只用一台设备写」——这既是产品文档 §9.3 的要求，也是存储层的安全边界。
- 一行 `topics` = 一个 Mosaic Room = 一张画布。没有 Workspace 表。

---

## 6.5 先设计表面，再长 SwiftUI 壳【已定方向】

壳跟 Mosaic 一样，所以「页面」其实是 **侧栏 + 当前 Topic 画布**，不是设置/列表/详情。要设计的是这套壳在不同设备和状态下长什么样。

| 要画的表面 | 要回答的问题 | 先不画 |
| --- | --- | --- |
| **壳**（侧栏 Topic 列表 + 当前画布 + chrome） | 和 Mosaic 有多像；Topic 行显示什么；空侧栏 | 账号、iCloud、Workspace 分组 |
| **写作现场**（iPad 横、iPad 竖、Mac） | 该 Topic 的正文放哪、卡片怎么围、gutter 在哪 | 一张画布两份稿 |
| **专注模式** | 画布怎么让开、怎么回来；这是生存条件（产品文档 §6.3） | 过渡动画的每一帧 |
| **一张卡 + 一根带类型的线** | 线形跟 Mosaic circuit；卡上能看出引用 / 依据 / 反驳 / 待解决 | 一百张卡的密度秀 |
| **Soundtrack pill + picker** | 整 app 一首，pill 在 chrome 里，不挡住打字；换 Topic 歌不变 | MusicKit 授权失败的每一种文案 |
| **空 Topic / 恢复后的现场** | 新题目 vs 昨天的光标和未解决问题 | 导出分享 sheet 的视觉抛光 |

先画、再写壳，为的是 S0 不要先长出一套随便的 NavigationSplitView，回头再拆。不挡 Mosaic 的 M0，也不挡 S2 的 IME 探针（那两件事设计稿验证不了）。

视觉语言：**壳的结构跟 Mosaic 走（侧栏、房间、chrome、jump），里面的密度按长时间写中文来，不要把终端 tile 的标题栏原样搬过来。**

设计稿的家：本仓库 `Design/`（Mosaic 用离线 HTML 板，这里也可以；Figma 也行，定稿要有一份能进 git 的板）。画完写作现场三端，再让 S0 的窗口壳去对齐它。

---

## 七、Spike 计划（按可杀死的顺序）

每一刀只回答一个不确定性。失败就停，不要平行加功能。

| ID | 问题 | 最小做法 | 失败长什么样 | 失败了怎么办 |
| --- | --- | --- | --- | --- |
| **S0** | 双平台工程能否空转 | SwiftUI app，iPad + Mac 两个 destination，SQLiteData 建 `topics`，能插入一行 | 其中一个 destination 编不过 | 停，先修工程，不谈画布 |
| **S1** | swift-flow 在两个平台上的平移/缩放是否可用 | 空白 `FlowCanvas` + 3 张普通卡片 + 1 条边；鼠标滚轮、触控板、捏合 | Mac 上滚轮不动；iPad 上和系统手势死锁 | 丢掉 swift-flow，改自己的 `Canvas`，或降级 sidebar |
| **S2** | 中文 IME 在 CM6+WKWebView 里是否可写 | 中央 overlay 编辑器，打 500 字中文，含标点、候选、撤回；composition 期间不落盘 | 吞字、死锁、marked text 被当成正文 | 见 §4.2；**这是唯一可以改变编辑器内核的闸门** |
| **S3** | 画布手势是否抢走编辑器滚动 | 编辑器里滚动正文，空白处平移画布，两者同时存在 | 两者抢 pan | 先锁死「编辑器命中时画布不平移」；再考虑是否把编辑器改成 LiveNode |
| **S4** | 从段落拉线是否做得出 | gutter 起手 → 屏幕点 → 画布点 → 落到一张卡上生成 `edges` 行 | 坐标对不上，或和选区打架 | 键盘等价先做（产品文档要求必须有）；手势后置 |
| **S5** | 现场能否恢复 | 打字、挪卡、关 app、再开：正文、光标、视口、展开的卡还在 | 丢字，或恢复了正在进行的请求 | 修落盘；不要在这之前做 AI |
| **S6** | 一个 AI 动作是否形成驻留 | 对当前段落提问 → 问题卡 + 回答卡落在附近 | 卡飘走、或写进了正文 | 修命令层；证明《AI 语义面》§一 |
| **S7** | 公式预览是否够用 | KaTeX 预览当前缓冲 | 渲染错、或拖垮 IME | 预览后置；源码模式本身已能写 |

S6 对应产品文档 §13 的两个验证问题。S6 之前的全部都是为了让「真实写作两周」有一个不会先被编辑器绊倒的探针。

**明确不进第一轮 spike：** CloudKit、MCP 服务、跨文检索、表格、图片粘贴、Zotero、多窗口、Catalyst、MusicKit（S6 通了再接）、把 MosaicPackage 链进工程。连线路由文件可以在 S4 **抄进来**，那不算「链进 MosaicPackage」。

---

## 八、和产品文档的对账

| 产品文档 | 本文 |
| --- | --- |
| §8.4 倾向 C（Mosaic room） | **改写为 A**：新 app。C 在「不用 AppKit」之后不可行。写回产品文档 |
| §8.3 编辑器是工作量最大头 | 用开源内核把它从「自研 TextKit」降成「选型 + IME spike」 |
| §3 中央线性正文 + 周围画布 | 正文 overlay，卡片走 swift-flow，两者不共享缩放 |
| §5.2 持久的带类型的边 | SQLite `edges` 是源；`FlowEdge` 是投影；**线形走 Mosaic circuit** |
| §8.2 从 Mosaic 借鉴 | 连线形态、拖线、注意力、MusicKit、LOD、命令目录、数据层模式；不搬 AppKit 视图 |
| §6 从正文拉线 | S4；键盘等价同时存在 |
| §9 两周真实写作 | 工程进度 = 不确定性减少，不是功能数。S6 通了才进入观察窗口 |
| §12 不为未来规模提前设计 | 同步只把表准备好，不实现；100 张卡、跨文、多人都不做 |

---

## 九、明确不做（本轮工程）

- 不写 AppKit 画布，不 fork Mosaic 的 app target。
- 不把 MosaicPackage 加进新 app 的 dependencies。
- 不启用 SyncEngine，不申请 iCloud / Screen Recording。
- 不做 MCP 传输，不托管外部 agent。
- 不自研 markdown 编辑器，不做 WYSIWYG，不做公式面板。
- 不把正文默认做成可缩放的 `FlowNode`。

---

## 附：还没钉死、但会改变下一刀的

1. 【待定】中央正文是 overlay、分栏，还是「写作态锁 1:1 的 LiveNode」。S3 之后才能选。
2. 【待定】swift-flow 的 iOS 26 下限能不能降。
3. 【待定】若 S2 失败，是否接受 iPad/Mac 两套编辑器内核。
4. 【待定】视口恢复还是「滚回最后编辑的段落」（数据模型 §8.2，产品文档问题 3）。
5. 【待定】打开 CloudKit 时，正文这一列的 last-write-wins 有没有可接受的产品包装（见数据模型 §十二）。
6. ~~Soundtrack 作用域~~ **已定：整 app 一首**（产品文档 §8.2）。

---

## 十、开发流程：两 app 并行【已定方向】

先把三个都叫「workspace」的东西拆开，后面才不会把工具用错：

| 名字 | 是什么 | 解决什么 |
| --- | --- | --- |
| **Xcode workspace** | 一个 `.xcworkspace` 里挂两个 project + 共享 package | 打开工程时两边的代码、文档、跳转都在 |
| **Git worktree** | 同一仓库的第二份工作目录、通常另一条分支 | 两份未提交改动互不覆盖 |
| **Agent 会话** | 一次对话的 `cwd` + 该目录的 `AGENTS.md` | 模型读哪份规则、改哪棵树 |

**默认不要开两个 git worktree。** 日常是：一个 checkout、一个 Xcode workspace、两个 scheme。Worktree 是「两条线同时脏」时才用的工具，不是常驻形态。

### 10.1 阶段

**现在（研究期，两份 git 仓库）**

- write-field 会话只在本仓库改文档和将来的 spike。
- Mosaic 当只读参考：读文档、读纯 Foundation 文件、按需抄，不在 write-field 会话里改 Mosaic 的 AppKit。
- 要修 Mosaic 本身，另开 Mosaic 目录的会话。不要在一次对话里同时改两个仓库的产品代码。

**迁进同一仓库之后（产品期）**

```
<repo>/
  <Name>.xcworkspace          ← 唯一 Xcode 入口
  Mosaic/                     ← 现有 macOS app
  WriteField/                 ← 本产品；自己的 AGENTS.md
  MosaicPackage/              ← 可移植 product 声明 iOS+macOS
  AGENTS.md                   ← 根：只负责分流，不把 Mosaic 的 AppKit 禁令套到 WriteField
```

- 打开根 workspace，用 scheme 切换 Mosaic / WriteField。DerivedData 用 workspace 默认那一份就行。
- 改 `WriteField/` 或只影响 WriteField 的代码：一条分支，CI 只跑 WriteField scheme + 可移植 package 测试。
- 改 `Mosaic/` 或只 macOS 的 Canvas：另一条分支，CI 只跑 Mosaic。
- **改可移植 package（路由、几何、attention 词汇）：** 当成第三个产品。同一 PR 必须两边都能编；测试至少跑 Core 测试 + Mosaic 里用到这些符号的测试。不允许「为了 WriteField 改路由、Mosaic 的线第二天歪了」。

### 10.2 什么时候才开第二个 git worktree

只在这几件事叠在一起时：

- Mosaic 在发版 / 修公证，工作区不能脏；
- 同时 WriteField spike 还要连续改几天；
- 或者可移植库的改动需要在「Mosaic 主线」和「WriteField 实验分支」上各编一次。

做法：`git worktree add` 出第二份**完整** checkout（不是只 checkout 一个 app 目录）。每份有自己的分支、自己的 DerivedData（Xcode 里把 DerivedData 设成 Unique，或 `-derivedDataPath .derived`）。两份都打开同一个 `.xcworkspace` 文件名可以，但不要让两个 Xcode 窗口写同一份 `xcuserdata`。

不要：

- 为两个 app 常驻两个 worktree（DerivedData、SPM checkout、索引会加倍，agent 也容易改错树）；
- 用 worktree 代替 Xcode workspace（它不共享跳转和 scheme）；
- 两个 worktree 挂同一条分支（git 不允许，也没有意义）；
- 在 A 树改可移植库、在 B 树不 rebase 就继续编 WriteField。

### 10.3 信息怎么共享、怎么隔离

共享（这是迁仓库的理由）：

- 同一份 circuit 文档、同一份 `WorkspaceWireRouter`、同一份 MusicKit 绑定模型；
- agent 在 WriteField 任务里可以直接打开 Mosaic 的 `Documentation/` 和 Core 源文件；
- 根 `AGENTS.md` 写清「WriteField 任务读 `WriteField/AGENTS.md`，Mosaic 任务读原来的 Mosaic 指南」。

隔离（这是两个 project / 两个 scheme 的理由）：

- 独立 bundle id、签名、entitlements、审核、Sparkle（Mosaic 有，WriteField 没有）；
- Mosaic 的 AppKit 禁令、Companion parked、Tour film 规则**不适用于** WriteField；
- 一次 agent 会话只认一个主 app。跨 app 只许读共享 package 和对方文档，不许顺手改对方 UI。

### 10.4 分支习惯

| 改动落在哪 | 分支 | 合并门槛 |
| --- | --- | --- |
| 只 WriteField | `write-field/…` | WriteField 编过，spike 验收见 §七 |
| 只 Mosaic | 现有 Mosaic 习惯 | Mosaic 测试 + 公证流程不动 |
| 可移植 package | `shared/…` | **两个 scheme 都编过**，Core 测试绿 |

WriteField 的「脏原型可以删」仍然成立（产品文档 §12.2），但共享库不是原型：进去的文件按 Mosaic 的标准看。宁可再抄一份实验路由，也不要在共享 product 里试错线形。
