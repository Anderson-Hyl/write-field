# write-field · 技术规划 v0.1 · AI 语义面

> 本文回答：**AI 在这个产品里，技术上到底能做什么、能写什么、不能写什么。**
>
> 产品文档的两条地板规则（§一）是本文的全部出发点：
> 1. **AI 是助手，不是主人。进不进正文，完全由作者决定。**
> 2. 如果一个功能在你**不写作**的时候也有用，那它不属于这个产品。
>
> 本文的立场：这两条不能靠提示词和自觉，要**做成类型系统的性质**。

> **2026-09-19 阅读状态**：产品目的已补充为帮助作者理解自己写下的内容；理解本身由作者判断，**不作为设计通过标准**。本文既有操作集与 §九的待定提案尚未统一升级；专项审视及建议决策表见 [AI 方案审视](research/ai-design-review-2026-09-19.md)。§三仅校正技术保证，§六重新标记凭据方案的未决项，其它建议不自动成为工程定案。

---

## 一、总原则：读得宽，写得窄【已定】

| | 能力 |
| --- | --- |
| **AI 能读** | 正文、锚点、卡片、边等可读对象；实际范围受作者本次授权和请求拼装限制。“读得宽”不代表可任意跨篇或默认读取全部材料 |
| **AI 能写** | **只能写卡片和边。** 图表、表格、候选片段也是卡上的内容，不是直接改纸 |
| **作者能写正文** | 手势：从卡插入一块（图表 / 表格 / 片段），或挂 footnote。这是作者命令，不是 AI 命令 |
| **AI 能判断** | 只能判断"这段话有什么问题"，**永远不判断"这段话是不是真的"** |

第一条是这个产品最重要的工程性质。它把 §一 的地板规则从"我们不打算做"变成"**做不出来**"：

> AI 面对的接口里，没有 `body.write` / `body.insert` / `body.replace`。
> 插入正文的 case 只挂在**作者手势**上（`author.insertFromCard`）。模型升级也调不到它。

## 二、操作集【已定】

语义命令层，小、少、都是数据操作。这一层直接继承 Mosaic 的经验 ——
`AGENTS.md`："keep MCP operations small, semantic, and data-oriented"；
`Architecture & Data Model.md`："MCP/CLI 操作语义化数据，不模拟鼠标"。

### 2.1 读

| 操作 | 返回 | 用途 |
| --- | --- | --- |
| `scope.read(topicID, anchorID?)` | **当前段落 + 前后邻段 + 已挂在这段上的卡与边** | 深读层。不喂整篇正文，除非作者显式要求 |
| `article.outline()` | 反推出的结构（标题路径 + 每段首句 + 各段挂载的卡数） | "看森林，不改树"；**默认请求拼装里常在**（见 §九） |
| `topic.stance(topicID)` | 作者写的短姿态文本（可空） | Topic 级意图；不是对话线程 |
| `corpus.search(query)` | 自己的文章 + 卡片里的命中 | **检索的第一顺位** |
| `card.list(topicID, filter)` | 卡片集合 | 未完成念头清单等视图 |

`scope.read` 的默认范围是刻意收窄的。产品文档 §七 要求默认安静、等待作者请求——工程上的对应物就是：**默认正文深读作用域是一个锚点，不是一篇文章。** 全貌靠 `article.outline()` 渐进变宽，不靠默认塞全文（拼装规则见 §九；其中始终携带全篇首句的范围提示问题，见专项研究 §六）。

### 2.2 写（注意：全部产出卡片或边）

| 操作 | 产出 | 备注 |
| --- | --- | --- |
| `question.create(anchorID, text, trigger, sourceCardIDs?)` | 问题卡 | 高频入口。`sourceCardIDs` 有值时，对每张来源写一根 `derived` 边 |
| `answer.create(questionCardID, body, provenance, trigger)` | 回答卡 | **必须挂在某张问题卡下** |
| `annotation.create(anchorID, type, body, provenance, trigger, sourceCardIDs?)` | 批注卡 | `type` ∈ 反驳 / 隐藏前提 / 模糊词 / 节奏 |
| `clip.create(provenance, title, selectedText)` | 资料卡 | `provenance` 只能是 `clipped` 或 `retrieved`。可以暂无锚点 |
| `snippet.create(text, trigger, anchorID?, sourceCardIDs?)` | 碎片卡 | 作者手写为主；从资料拆出的碎片必带来源边。可承载待插入的 mermaid / 表格 / 片段 |
| `edge.create(type, source, target, note?)` | 带类型的边 | 见数据模型 §六 |
| `card.setState(cardID, state)` | 状态流转 | 草稿 / 待用 / 已用 / 废弃 |

从卡发起时，`anchorID` 取来源卡的锚点；来源无锚点则取当前 caret 所在段。两者都没有 → 命令失败，不造游离卡。没有 `card.summarize` / `card.digest`。

### 2.3 明确不存在的操作【已定】

这几条不存在，是有意的：

- ❌ AI 调用 `body.write` / `body.insert` / `body.replace` / `body.rewrite` —— 助手不能当主人
- ✅ 作者调用 `author.insertFromCard` / `author.footnoteFromCard` / 自行改写 / 废弃 —— **回填路径由作者选**，见 §2.5
- ❌ `truth.verify` / `source.validate` —— 工具不扮演裁判（§二）
- ❌ `scan.schedule` / `watch` / `onIdle` —— **AI 子系统没有定时器，没有观察者入口**
- ❌ `answer.autoAttach` —— 回答卡不能自己找地方待着，它待在被问出来的那个锚点旁
- ❌ `card.summarize` / `card.digest` / `corpus.process` —— 对资料「处理」只能是问、反驳、拆前提；不能写成另一篇正文
- ❌ 对一组 **无锚点** 的卡跑 AI —— 先挂到某段，或对当前段提问时带上这些卡
- ❌ 静默刮取参考页 DOM 进模型上下文 —— 无作者剪取手势则网页内容不进 AI 窗

### 2.5 回填交给作者【待定方向，2026-09-19】

AI 产出停在卡上之后，**进不进纸、怎么进、进不进策展包，都不由模型选**。UI 把选择摊开，禁止默认「一键已写入正文」的暗模式。

| 作者选择 | 效果 |
| --- | --- |
| **插入块** | `author.insertFromCard` → 图表 / 表格 / 代码围栏 / 片段进 caret（原型不做选区 Replace） |
| **作 footnote** | 出处挂进 markdown footnote，资料卡变已用 |
| **只留在旁** | 卡 `ready`，纸不变 |
| **自行改写** | 作者盯着卡打字；卡可后标已用 |
| **废弃** | `discarded` + 可选「为什么放弃」 |
| **部分采用**【待定】 | 只插入卡内选区，不是整卡 |
| **钉进 / 移出策展** | 只改读上下文勾选，不改纸 |

「回填」包括两层，都交给用户：**(1) 回填正文；(2) 回填后续 AI 读窗（策展）**。模型不能 `body.*`，也不能自己 `card.pinToContext`。

### 2.4 为什么"没有定时器"是一条架构约束

产品文档的交互顺序原则是"**先让我怀疑，再让机器核对**"，并点明了反模式："每次写完自动全篇检查 → 作者会逐渐不再自己核对"。

这条在架构上的表达就是：**AI 子系统没有除"作者手势"以外的任何入口。** 没有定时器、没有文件观察者、没有"写满 N 字就触发"。每个 AI 产生的对象都带一个 `trigger` 字段，记录它是哪次手势的产物。这既让"机器只在被怀疑时才核对"成为事实，也让三个月后能看出"这张卡是我在第三段起疑时问出来的"。

**【规则冲突校正，2026-09-19】** 原“重要文档自动模式”例外与地板冲突，撤下该例外；作者显式请求一次全文审阅仍是合法入口，不是授权以后自动监听或定时运行。产品文档 §七同步校正。不接系统 Writing Tools 的 Rewrite / Compose（产品文档 §3.7）。作者要插入图表，走 `author.insertFromCard`，不走 Writing Tools。

## 三、引用完整性：来源可追溯，不认证事实【地板已定；技术保证修订，2026-09-19】

数据模型 §五 的 `clipped | retrieved | asserted` 与 `asserted` 不可作为 footnote / 引用边目标的地板保留。**撤回旧的“三态 + URL 实访使编造引用无法构造”保证**：模型可以提供真实 URL 和虚构引文；可达只说明获取状态。结构化输出也不保证内容正确。[官方说明](https://developers.openai.com/api/docs/guides/structured-outputs)

既有边界是：资料卡必须来自真实剪取或检索，原文不能被模型转述替代，没有来源就明确没有；不建立真假字段。

以下构造机制【待定】，须与数据模型一起定案：

1. 剪取 / 检索执行器生成来源回执和 `sourceID`；模型不能直接声明自己的输出是实际取得的原文。§2.2 的 `clip.create(provenance, ...)` 须改为接收可信来源引用，不能原样暴露为模型自证接口。
2. 应用验证 ID、授权范围、片段是否存在及引文定位；这只能验证来源链和字面匹配，不能证明资料或论断为真。搜索摘要、页面节选与完整原文要区分。
3. AI 综合与解释保留为模型产物，可连接多份来源；不因有来源就将整段生成文字升级成原文资料。
4. 区分从未取得材料与已保存材料后来不可达。旧“网络失败一律降为 asserted”规则撤回待重新建模；不能把已发生的真实剪取改成模型断言。具体可用性字段未定，不在 schema 临时加第四态。

详见 [专项研究 §七](research/ai-design-review-2026-09-19.md#7-来源链要保护什么)。这次是技术表述校正和实现提案，尚无已运行的来源验证器。

## 四、回答卡的驻留【已定方向，细节待定】

产品文档 §四 的核心机制是"AI 的产物永远先落在纸的周围"。它有两层含义，工程上是两件事：

**认知减速**（不自动落进正文）—— 已经由 §一 的写权限收窄保证。

**上下文驻留**（回答不随聊天时间线滚走）—— 需要回答卡有"归属"：

- 回答卡 → 必属于一张问题卡（`parentCardID`）；
- 问题卡 → 必属于一个锚点（`anchorID`）；
- 从资料卡长出的问题 / 批注 / 碎片 → 继承来源锚点，并各有一根 `derived` 边；
- 因此回答卡**自动获得一个正文位置**，而不需要一个聊天窗口。卡再长出卡也不许变成第二份聊天。

由此推出两条实现规则：

1. **回答卡不允许被"移动"到与它的问题无关的位置后再独立存在。** 允许作者把它拖远（工作需要空间），但归属不变，双向高亮仍然按归属工作。
2. **周围不存在"游离的对话"。** 如果一个交互产生了没有锚点的 AI 输出，那它设计错了。

这也顺带解决了产品文档 §十一 的"AI 产出堆积"风险：产物有归属，就自然会被"这一段有几张卡"的组织方式收敛，而不是堆成一条越来越长的流。

## 五、检索顺序：先自己的语料，后外部【已定方向】

产品文档 §六 明确写了"**先搜我自己的语料**，再外部"。这既是产品判断，也是工程上的成本优势：

1. `corpus.search(query)` —— 本地 FTS5，同步、免费、离线可用。命中直接出资料卡（`provenance.kind = clipped`，因为是你自己的东西）。
2. 只有在作者显式要求"往外找"时，才发起外部检索。结果进 `retrieved`。

【待定】跨文重复 / 矛盾检测需要语料索引的成熟度，产品文档把它标为"长期护城河"。本原型不做，但 FTS5 索引从第一天就建，免得将来补。

## 六、模型接入与失败姿态

| 项 | 决定 |
| --- | --- |
| API key | 【待定，2026-09-19 重开】原“应用自己持有、直连”未区分用户自带 key 与产品服务 key。不得将开发者共用 key 打包进客户端；BYOK 的存储与风险、产品付费调用的服务端方案须先决定。直连仍为原方向，不据此自动引入中转服务，见专项研究 §9.1 |
| 传输 | 纯 HTTPS 请求，沙盒内合法；无本地进程、无 MCP 宿主 |
| 可用性 | 无网 / 无 key / 模型不可用 = **产品仍完全可用**，只是 AI 手势给不出卡。写作本身不依赖任何网络【已定】 |
| 成本控制 | 一次手势 = 一次请求 = **有上限的产物数**。默认建议按 `trigger` 分级（见 §9.6）：澄清类 1 张；反驳 / 拆前提 / 定向结构 ≤3。不做后台扇出 |
| 可审计 | 每张 AI 卡带 `provenance.model` + `trigger`，作者能看出这张卡是谁、什么时候、因为什么产生的 |

### 6.1 MCP 在这个产品里是什么【待定】

Mosaic 的 MCP 有明确用途：让外部 agent（Claude Code / Codex）往房间里放卡。write-field 在 iPad 上**没有宿主外部 agent 的合理场景**——沙盒、无进程、无本地监听。

所以本轮的判断是：

- **语义命令层照做**（§二），因为它是应用内 AI 功能与数据层之间正确的分界；
- **MCP 传输层先不做**。将来如果 macOS 版要接外部 agent，再把同一层包一个 MCP 面，**不重新设计操作集**。

这个判断的价值在于：不做 MCP 服务，但把"MCP 式的小而语义化的命令"这个纪律保留下来。

## 七、和产品文档的逐条对账

| 产品文档的要求 | 本文的工程对应 | 状态 |
| --- | --- | --- |
| AI 是助手不是主人；进不进正文由作者决定 | AI 无正文写操作；作者有 `insertFromCard` | 【已定】 |
| 不判断内容真伪 | 无 `truth.verify`，无真假字段，只有"从哪来" | 【已定】 |
| 不自动插入任何内容 | 唯一入正文路径是作者手势：插入块或 footnote | 【已定】 |
| 引用须来自真实剪取或检索 | `asserted` 进不了引用位；来源回执与片段校验待定；不承诺来源或论断为真 | 地板【已定】；保证修订见 §三 |
| AI 产物先落在卡上（含可插入的图表 / 片段） | 写操作集只有卡片与边 | 【已定】 |
| AI 默认安静，不主动扫全篇 | 无定时器 / 无观察者 / 正文深读 = 一个锚点；全貌靠 outline | 【已定】+ §九【待定】 |
| 先让我怀疑，再让机器核对 | AI 的唯一入口是作者手势；每卡带 `trigger` | 【已定】 |
| 先自己的语料，后外部 | `corpus.search` 在前，外部检索需显式要求 | 【已定方向】 |
| AI 产出堆积的风险 | 产物必有归属（回答 → 问题 → 锚点）；一手势产物数有上限 | 【已定方向】 |
| 读上下文何时加宽 | 缺口 → 显式手势；默认只够当前段 | §9.0【待定】 |
| 开写意图 / 旁路提问 | Stance + stancePolicy；正交于拼装模式 | §9.2【待定】 |
| 正文渐进进入模型上下文 | progressive：提纲常在 + 当前段深读 | §9.1【待定】 |
| 知道缺什么 / 不知道缺什么 | curated 策展 ↔ full 刷新；可回渐进 | §9.1【待定】 |
| 查阅 vs 思辨、产物上限、钉进策展 | 双题演练修订 | §9.6【待定】 |
| AI 变强会不会让它贬值 | 价值在"读、质疑、记住、保留上下文"，这些不会随模型变强而消失 | 立场，非工程项 |

## 八、明确不做（本轮）

- 不做 MCP 服务端 / 不做外部 agent 接入。
- 不做 embedding、不做跨文语义检索。
- 不做自动全篇体检、不做"重要文档"自动模式；无定时器或观察者接口位。
- 不做多模型编排、不做 agent 工具循环。
- 不做任何形式的"AI 协作写作"（边写边补全）。
- 不做开写向导二选一锁死「AI 人格」；不做纸上方第二聊天线程当上下文容器。

---

## 九、请求上下文：缺口驱动的加宽【待定】

> 2026-09-19 讨论落入，同日用「现场缺口 → 显式手势」再检视后重写结构。全部【待定】。  
> 写权限仍完全服从 §一～§二（只写卡与边；无定时器）。本章只回答：**读上下文何时窄、何时宽、谁决定加宽。**

### 9.0 底层原则（从「刷新」那次灵感反推）

「刷新写作上下文」不是功能清单上的一项，它暴露了拼装层该怎么长出来：

1. **先看见写作现场里的一个具体缺口**（AI 没看见前面某段 / 某张卡 / 某份资料）。  
2. **再给一个作者手势**，只加宽读上下文，不改写权限、不自动跑审稿。  
3. **默认保持够用且安静**；加宽可逆、结果可见、超预算明说截断。  
4. **禁止**用后台同步、定时器、或「模型自己觉得该看全文」来加宽。

一句话：

> **读上下文的默认值只够写当前这一段；任何加宽都是作者发现缺口之后的显式动作。**

因此底层不是「开写选模式 / 一个总 prompt / 一场聊天」，而是一张**缺口 → 手势**表。对象（Stance、ContextPack）是手势的持久化结果，不是入口。

| 现场缺口 | 作者手势 | 读上下文怎么变 | 写权限 |
| --- | --- | --- | --- |
| 这段起疑 / 想辨一层 | 侧边拉线 → **松手在召唤球**才进 AI；空白=手写旁注 | 渐进：提纲 + 当前段深读 | 卡与边 |
| 只要这几张卡 / 那几段 | **策展上下文**（含卡上钉进） | `curated`：作者勾选的段与卡 | 同上 |
| 不知道缺了什么，但明显不对 | **刷新写作上下文** | `full`：尽力纳入本 Topic 已有正文与卡 | 同上 |
| 记不清事实 / 签名 / API（想开 Hoogle） | **参考页卡**（旁挂 Web）或查阅→资料卡 | 活页可看；剪取后才进 AI 窗 / 引用 | 同上 |
| 想跑题问一句（用词、中译） | 旁路（btw） | 窗不变，本次不带 Stance | 同上 |
| 这篇要交付什么不清楚 | 写 / 改 Stance（随笔常中途 amend） | 软约束进窗（可空） | 不改纸 |
| 不知从哪下笔 | 定向：挖问题 / 反推章节 | 仍挂空段或首段；产章节卡 / 问题卡 | 同上 |
| 有资料、纸还空，想先问非目标 | **空标题挂点** + 卡，或「当前段 + 这些卡」 | 点名附件进窗 | 无锚点堆不能开写 |
| 改完段，旧回答还在说话 | **对现状再问**（§9.3，轻） | 用当前正文深读重问；旧卡可标过时 | 新卡，不改旧卡正文 |
| 跨另一篇 Topic 借一句 | `corpus.search` / 手搓剪藏 | **不**靠本 Topic 的 ContextPack | 资料卡 |
| 加宽加错了 / 太贵 | 回到渐进 | `progressive` | — |

时间轴仍是 Orient → Compose → Stress（产品 §3.5）。Orient 长短不同（围着主题 vs 带着 PRD 资料）**不是**两种 AI 后端，只是上表谁先出现。周六双题演练见 §9.6、`research/saturday-dual-session-ai-audit.md`。

禁止：空纸 Compose 进正文；对无锚点资料堆 `corpus.process`；默默把默认窗加宽冒充「更聪明」；用刷新假装解决跨 Topic 记忆。

### 9.1 三种拼装模式（并列）

`contextMode` ∈ `progressive | curated | full`。同一时间 Topic 只有一个模式；切换都是作者手势。

| 模式 | 何时用 | 进请求的正文 | 进请求的卡 |
| --- | --- | --- | --- |
| **progressive**（默认） | 日常起疑 | `outline` + 当前段与邻段全文 | 当前段已挂卡 + 本次点名 `sourceCardIDs` |
| **curated** | 作者**知道**缺什么 | `outline` + 勾选的块全文（未勾选的段不深读） | 勾选的卡（可含 inbox、可含别段的卡） |
| **full** | 作者**不知道**缺了什么 | `outline` + `body` 尽力全文 | 未废弃卡尽力全收（含 inbox 只读） |

关系：

- 策展不是刷新的马甲：刷新是「不知道缺什么」的逃生口；策展是「我点名」。结构化文（PRD）演练里**策展远比 full 常用**；随笔迷路时 full 才更像逃生口。  
- 刷新可以做成「打开策展 UI 且预勾全部」的快捷动作，但模式值仍记 `full`，以便审计与「包已旧」提示。  
- 单次提问上的 `sourceCardIDs` 仍可用：在 progressive 下等于一次性微策展；若发现自己反复点同一批卡，应升格成 `curated` 包，而不是每次手点。  
- **卡上钉进 / 移出策展**【待定优化】：比每次打开策展清单更跟手；钉选变更即更新 `includedCardIDs`。  
- full/curated 超预算裁剪时，**降权从未展开过的卡**（Orient 一次性章节卡、空话验收卡常在此列），避免刷新把噪音全喂回去。

#### progressive 拼装

```text
[stance?]                    ← inherit 时；ignore 则跳过
+ article.outline(topic)     ← 常在；随稿变丰 = 「正文渐进进入全貌」
+ scope.read(anchor)         ← 当前段全文 + 邻段 + 已挂卡与边
+ attached cards             ← 本次 sourceCardIDs
+ author question / trigger
```

硬约束：禁止默认喂全文；提纲可重建；段旁卡是便宜记忆。稿极短、提纲几乎为空时，深读层占主导——若仍不够，作者策展或刷新，**不**在渐进里静默加段。

#### curated 拼装

```text
[stance?]
+ article.outline(topic)
+ 勾选块的正文全文
+ 勾选卡全文 + 其间的边
+ author question / trigger
```

勾选集持久在 Topic 旁路（`includedCardIDs`、`includedAnchorIDs` 或 block 指纹）。作者可改勾选而不必先回渐进。空勾选 = 非法，保存时至少保留当前段。

#### full 拼装（刷新写作上下文）

```text
[stance?]
+ article.outline(topic)
+ topics.body                 ← 超预算则截断并标记
+ card.list(not discarded)    ← 超预算：未解决问题 > 当前段挂载 > 最近更新
+ edges among included
+ author question / trigger
```

刷新默认**不打模型**；只重建读包。`packedAt` + `bodyHash`；body 变更只提示「包已旧」，不自动再刷。必须能「回到渐进」。截断可见。刷新 ≠ 全篇体检 ≠ `corpus.process`。

Glow：本地打包不必 AI glow；若作者另要「压成摘要卡」，glow 只包住那张卡，不包纸。

### 9.2 Stance 与旁路（正交于模式）

Stance / btw **不是**第四种 `contextMode`。它们回答「姿态带不带」，不回答「正文与卡看多宽」。

| 层 | 寿命 | 谁写 | 作用 |
| --- | --- | --- | --- |
| **Stance** | Topic 级，可选 | 仅作者（AI 提议须先落卡再确认） | 交付物 / 读者 / 硬约束；软注入 |
| **stancePolicy** | 单次手势 | 作者 | `inherit` / `ignore`（btw）/ `amend` |
| **contextMode** | Topic 级直到改回 | 作者 | progressive / curated / full |

Stance 建议 ≤2KB，不进 `.md`、不进纸，可空。旁路不另开聊天窗。

### 9.3 检视中多出的轻量点子（先记，不抢三种模式）

1. **对现状再问**【待定】  
   缺口：段已改写，旧回答仍挂在失效或过时锚点旁。  
   手势：对问题卡「按当前正文再问」→ 新回答卡；旧卡可标废弃或保留对照。不自动重跑。  

2. **借阅进窗**  
   缺口：卡锚在第 2 段，人在第 5 段写，只想让下次请求读到那张卡。  
   先靠 `sourceCardIDs` / 卡上钉进策展吸收；不增第四模式。

3. **查阅 vs 思辨**【待定方向，周六演练】  
   缺口：开 Hoogle / 文档只为一行事实，回答卡却当思辨驻留，难引用。  
   手势分流：能打开参考页则先打开；冻结引用走 `clip.create`；思辨走 `question`/`answer`/`annotation`。btw 中译仍可短答。

4. **参考页卡（旁挂 Web）【待定方向，2026-09-19】**  
   缺口：写 Haskell 时要 Hoogle，离开 App 切浏览器丢现场。  
   形态：**锚定在段落旁的参考页卡**（内嵌 `WKWebView`），不是 Mosaic 自由 Web tile。  
   - 借：Mosaic 用 `WKWebView` 看网页的可行性（iPad 也有）。  
   - **不借**：`WebNodeView` 工作台浏览器、多开任意地址栏漫游、房间相机。  
   - 可预填查询（选区 → Hoogle URL），作者仍可改 URL。  
   - **刷新【待定 → 倾向已定】**：作者可点「刷新」重载当前页（改了 Hoogle 查询、文档更新后常见）。这是**作者手势**，不是定时器 / 观察者 / 聚焦自动刷。语义上可记为 `reference.reload(cardID)`，与 AI 子系统的 `watch`/`onIdle` 无关。  
   - **活页 vs 冻结**：参考页是 live；要进引用 / AI 读窗，须作者选中再「剪成资料卡」（带 URL provenance）。禁止后台刮 DOM；刷新也不自动把新 DOM 推进模型。  
   - **风险（持异议）**：与纸争纵向滚动；working set 建议同时只展开一张参考页，其余收成端口。若演变成研究白板，降级为「系统浏览器 + 剪回」。  

5. **回填选择面**【待定方向】  
   见 §2.5。补充：长回答卡应支持「只采用选区」；插入后撤销须干净；UI 不把「插入」做成唯一大按钮。

6. **其它可补缺口（同级，先记）**  
   - **选区 → 参考页**：纸上选 `traverse`，侧边开 Hoogle 查询卡。  
   - **参考页 → 提问**：对当前页已剪片段提问（先有 clip），不是对整页 HTML 问。  
   - **Snippet 预览**：代码/图表插入前在卡上预览，减少插错围栏。  
   - **焦点**：打开参考页不偷走纸的打字焦点（尤其 Mac）；iPad 上明确「正在打字 / 正在看页」。  
   - **失败态**：无网时参考页明示；查阅可退回本地语料；刷新失败同样明示，不静默留旧页装成功。  
   - **角色色**：参考页走「资料」角色，不新开霓虹。

刻意不做的「点子」：模型自动摘要进隐藏 system prompt；按时间线重放全部对话；默认窗随字数偷偷变宽；开写强制填 Stance；无剪取就把网页全文塞进模型；周围变成 Mosaic 式多 Web tile 工作台；参考页定时自动刷新。

### 9.4 和已定规则的边界

- 无定时器 / 无观察者：稿变长不加宽、不刷新、不重问。  
- 三种模式都只扩**读**；无锚点堆仍不能单独开写。  
- Stance / ContextPack 再强，无 `body.write`。  
- ContextPack **不跨 Topic**；跨篇走语料检索或剪藏。  
- 审计：AI 卡带 `trigger`、`model`、`usedStance`、`contextMode`（及 full/curated 的 pack 指纹）。

### 9.5 实现落点（S6，不提前）

- 数据：见数据模型【待定】注（`stance`、`contextMode`、`packedAt`、`bodyHash`、策展勾选 JSON）。Pack **不**另存第二份稿。  
- 运行时：`RequestContext.assemble(...)`；`context.refresh` / `context.curate` / `context.useProgressive`；`card.pinToContext` / `unpin`；`reference.reload`。  
- **UI 入口地图【待定方向，2026-09-19】**：A 纸侧边（问/查阅/反驳）· B 卡菜单（回填/钉/剪）· C Topic 轻量条（Stance、徽记、包已旧；非 nav bar）· D 命令目录（策展/刷新/回渐进）。禁止第五个 AI 抽屉。板：`Design/ai-入口地图.html`、`回填选择.html`、`参考页卡.html`、`ai-上下文与姿态.html`。  
- 推荐仪式（不新建模）：PRD 开写先打空标题挂点，再挂 inbox 卡提问。

### 9.6 周六双题真写演练 · 策略修订【待定方向，2026-09-19】

证据：`research/saturday-dual-session-ai-audit.md`（Haskell `Traversable` 导读 + Mosaic Jump Palette Recent/Pinned PRD）。

| 发现 | 策略改动 |
| --- | --- |
| 查签名 / API 会离开正文 | 参考页卡 + 剪成资料卡（§9.3.4）；回填选项交给作者（§2.5） |
| 开篇对照 laws、PRD 对照非目标 | 结构化文默认想 **策展**，不是 full |
| 澄清问却收 3 张卡 | **按 trigger 分级产物上限**（澄清 1；反驳/拆前提/定向 ≤3） |
| 章节卡、空话验收卡喂回 full | 裁剪时降权「从未展开」 |
| 随笔 Stance 后置、PRD Stance 前置 | Stance 可空；禁止开写向导；支持 amend |
| 有资料无正文想问非目标 | 空标题挂点仪式写入 §9.5 |
| 上午两篇之间借隐喻 | 明示 ContextPack 不跨 Topic |
| 能策展却点刷新并忘在 full | UI/文案强调策展优先；full 须易见「回到渐进」 |

仍未验证（演练是想象现场，不是两周真写）：钉进策展是否真比打开清单更常用；查阅/思辨分流作者是否选得准；分级上限是否误伤「一次要两个反例」。

---

### 9.7 从写作过程提出的异议【待定建议，2026-09-19】

作者本轮强调充分发挥 AI 的能力来帮助形成经过考量的文章，作者保持主导。新推演：[周六双场景再演练](research/saturday-writing-walkthrough-v2.md)，产品文档 §十四。以下是对现有约束的讨论请求，**不视为已生效命令或替换既有定案**：

1. 空稿的开题讨论可归属 Topic，而非必须先造文本锚点。需要与 §2.2 / §四的强锚点约束一同修订，不能只给 `anchorID` 填空值绕过。
2. 一张讨论卡承载连续追问，作者再提出有价值的材料；显式保留作者认可 / 拒绝及理由。原始记录仍可追溯，不暗中生成隐藏记忆改变作者意图。
3. 作者明确要求时，允许比较、归纳和解释所选资料，结果先在卡上且保留来源。重新讨论 §2.3 对摘要本身的禁止；不放开自动写正文。
4. “通读全文检查目标和验收”本身可构成这次全文阅读授权。选定范围在下一次作者请求时取当前版本；与请求历史快照分开，不要求改字后反复刷新包。越过本篇 / 已选资料的边界仍需作者明确选择。
5. 一次作者任务可以讨论采用有界多步读取 / 检索，显示范围、成本和取消，而非将一次 HTTP 请求视为产品边界。此项仅记异议，未决定实现工具循环，也不放开后台 AI。
6. 来源构造必须来自实际检索 / 剪取通路；URL 可达与类型字段不能证明引文或论断为真。§三已校正绝对保证措辞，来源回执实现仍待定，同时保持 `asserted` 不可引用的地板。

产品 §七与本文 §2.4 的“重要文档自动模式”例外已统一校正：作者主动请求整篇审阅，与自动监听 / 定时检查分开，服从无定时器、无观察者入口的地板。

### 9.8 对应 HTML 的请求规则提案【待定，2026-09-19】

产品 §14.1 与 [HTML 修改说明](Design/理解与作者主导-修改说明.md) 把 §9.7 的意见落实为可讨论的演示，仍未替换 §九旧三模式或 §四强锚点的工程约束。

- **当前版本与历史快照分离**：请求发出时读取作者本次选定范围，记录该次所读文本；下一次作者请求读取最新文本。输入与改稿不触发 AI，不后台刷新摘要。
- **范围按次明确**：当前段 / 所选章节 / 全文；可另外带指定资料与作者已保存的意图。未保存意图不自动带入。全文请求后默认回局部是待讨论的交互选择，不是已有策略的定案。
- **失效不猜**：演示里的标题改名、删除或重复会要求重选，不静默扩到全文或替换成别节。最终仍需真正的锚点机制。
- **讨论与取舍分开**：局部连续对话保留原问题与回答；作者取舍只有作者明确留下才成立。开题 Topic 归属待模型与命令约束统一论证后再进工程。
- **诚实的演示边界**：固定问法给固定示例答复；自由问题只记录，不伪造理解。全文阅读只产生范围回执，不伪造审阅完成；参考页刷新不显示为真实联网成功。

## 附：需要验证的假设（进入实现前）

1. 【待定】**progressive** 是否覆盖日常起疑；结构化文不够时是否真会先策展而非刷新。  
2. 【待定】Stance：随笔可空、PRD 常满；空 Stance 必须仍好用。  
3. 【待定】**按 trigger 分级产物上限**是否比一刀切 ≤3 更少「为产品而用产品」。  
4. 【待定】"草稿 / 待用 / 已用"与「是否展开过」是否被维护；否则 full 裁剪失真。  
5. 【待定】旁路 `ignore` 是否够用；查阅是否常被误做成 btw 短答。  
6. 【待定】`full` sticky 是否太贵；刷新是否应默认「预勾全选的策展」而少用 sticky full。  
7. 【待定】full/curated 截断优先级（未解决问题 > 当前段 > 最近更新 > 从未展开垫底）是否符合直觉。  
8. 【待定】「对现状再问」是否比手搓新问题更常出现。  
9. 【待定】卡上钉进策展是否值得做独立手势，还是 `sourceCardIDs` 已够。
