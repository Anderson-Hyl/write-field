# write-field · 技术规划 v0.1 · AI 语义面

> 本文回答：**AI 在这个产品里，技术上到底能做什么、能写什么、不能写什么。**
>
> 产品文档的两条地板规则（§一）是本文的全部出发点：
> 1. **AI 是助手，不是主人。进不进正文，完全由作者决定。**
> 2. 如果一个功能在你**不写作**的时候也有用，那它不属于这个产品。
>
> 本文的立场：这两条不能靠提示词和自觉，要**做成类型系统的性质**。

---

## 一、总原则：读得宽，写得窄【已定】

| | 能力 |
| --- | --- |
| **AI 能读** | 正文、锚点、卡片、边、自己的语料索引。范围几乎不受限 |
| **AI 能写** | **只能写卡片和边。** 图表、表格、候选片段也是卡上的内容，不是直接改纸 |
| **作者能写正文** | 手势：从卡插入一块（图表 / 表格 / 片段），或挂 footnote。这是作者命令，不是 AI 命令 |
| **AI 能判断** | 只能判断"这段话有什么问题"，**永远不判断"这段话是不是真的"** |

第一条是这个产品最重要的工程性质。它把 §一 的地板规则从"我们不打算做"变成"**做不出来**"：

> AI 面对的接口里，没有 `body.write` / `body.insert` / `body.replace`。
> 插入正文的 case 只挂在**作者手势**上（`author.insertFromCard`）。模型升级也调不到它。

## 二、操作集【已定】

语义命令层，小、少、都是数据操作。这一层直接继承 Mosaic 的经验 ——
`AGENTS.md`："keep MCP operations small, semantic, and data-oriented"；
`Architecture & Data Model.md`："MCP/CLI 操作语义化数据，不模拟鼠标"。

### 2.1 读

| 操作 | 返回 | 用途 |
| --- | --- | --- |
| `scope.read(topicID, anchorID?)` | **当前段落 + 前后邻段 + 已挂在这段上的卡与边** | 默认视野。不喂整篇，除非作者显式要求 |
| `article.outline()` | 反推出的结构（标题路径 + 每段首句 + 各段挂载的卡数） | "看森林，不改树" |
| `corpus.search(query)` | 自己的文章 + 卡片里的命中 | **检索的第一顺位** |
| `card.list(topicID, filter)` | 卡片集合 | 未完成念头清单等视图 |

`scope.read` 的默认范围是刻意收窄的。产品文档 §七 说"默认安静：AI 在后台准备，不主动扫全篇"——工程上的对应物就是：**默认的读作用域是一个锚点，不是一篇文章。**

### 2.2 写（注意：全部产出卡片或边）

| 操作 | 产出 | 备注 |
| --- | --- | --- |
| `question.create(anchorID, text, trigger, sourceCardIDs?)` | 问题卡 | 高频入口。`sourceCardIDs` 有值时，对每张来源写一根 `derived` 边 |
| `answer.create(questionCardID, body, provenance, trigger)` | 回答卡 | **必须挂在某张问题卡下** |
| `annotation.create(anchorID, type, body, provenance, trigger, sourceCardIDs?)` | 批注卡 | `type` ∈ 反驳 / 隐藏前提 / 模糊词 / 节奏 |
| `clip.create(provenance, title, selectedText)` | 资料卡 | `provenance` 只能是 `clipped` 或 `retrieved`。可以暂无锚点 |
| `snippet.create(text, trigger, anchorID?, sourceCardIDs?)` | 碎片卡 | 作者手写为主；从资料拆出的碎片必带来源边。可承载待插入的 mermaid / 表格 / 片段 |
| `edge.create(type, source, target, note?)` | 带类型的边 | 见数据模型 §六 |
| `card.setState(cardID, state)` | 状态流转 | 草稿 / 待用 / 已用 / 废弃 |

从卡发起时，`anchorID` 取来源卡的锚点；来源无锚点则取当前 caret 所在段。两者都没有 → 命令失败，不造游离卡。没有 `card.summarize` / `card.digest`。

### 2.3 明确不存在的操作【已定】

这几条不存在，是有意的：

- ❌ AI 调用 `body.write` / `body.insert` / `body.replace` / `body.rewrite` —— 助手不能当主人
- ✅ 作者调用 `author.insertFromCard(cardID, at: caret)` —— 把卡上的块插入纸（图表、表格、片段）。原型不做选区 Replace
- ❌ `truth.verify` / `source.validate` —— 工具不扮演裁判（§二）
- ❌ `scan.schedule` / `watch` / `onIdle` —— **AI 子系统没有定时器，没有观察者入口**
- ❌ `answer.autoAttach` —— 回答卡不能自己找地方待着，它待在被问出来的那个锚点旁
- ❌ `card.summarize` / `card.digest` / `corpus.process` —— 对资料「处理」只能是问、反驳、拆前提；不能写成另一篇正文
- ❌ 对一组 **无锚点** 的卡跑 AI —— 先挂到某段，或对当前段提问时带上这些卡

### 2.4 为什么"没有定时器"是一条架构约束

产品文档的交互顺序原则是"**先让我怀疑，再让机器核对**"，并点明了反模式："每次写完自动全篇检查 → 作者会逐渐不再自己核对"。

这条在架构上的表达就是：**AI 子系统没有除"作者手势"以外的任何入口。** 没有定时器、没有文件观察者、没有"写满 N 字就触发"。每个 AI 产生的对象都带一个 `trigger` 字段，记录它是哪次手势的产物。这既让"机器只在被怀疑时才核对"成为事实，也让三个月后能看出"这张卡是我在第三段起疑时问出来的"。

唯一允许的例外是产品文档 §七 提到的"重要文档"模式，但它仍然**只往四周的卡写，不打断写作、不自动进正文**，且必须由作者显式开启，默认关。不接系统 Writing Tools 的 Rewrite / Compose（产品文档 §3.7）。作者要插入图表，走 `author.insertFromCard`，不走 Writing Tools。

## 三、引用完整性：让"编造引用"无法构造【已定】

数据模型 §五 已经定了判别联合 `clipped | retrieved | asserted`。AI 语义面这条要补的是**动作级**的保证：

1. `clip.create` 的 `provenance` 参数**只接受** `clipped` 或 `retrieved`。模型无法为一张资料卡声明 `asserted`——它没有那个参数路径。
2. `retrieved` 形态要求 `resultURL` + `snippet` + `retrievedAt` + `engine` 同时存在，且 **URL 在创建时被真的请求过一次**。请求失败 → 卡片降级为 `asserted`，并在卡面上写明"来源不可达"。
3. **`asserted` 卡永远不能成为 footnote / 引用边的目标。** 它可以是提问的引子，不能是依据。UI 上显示为"无来源"。
4. 检索结果**原样保存**。不保存"模型对结果的转述"——那是引用失真的唯一入口。
5. 没有检索结果时，正确行为是**明说没有**（"没有来源"就显示没有，§七），而不是找一个相近的顶上。

> 合起来：`asserted` 卡在类型上就进不了引用位置，`clipped`/`retrieved` 卡必须带一个真的被访问过的 URL。**"编造引用"不是被禁止，是没有构造器。**

## 四、回答卡的驻留【已定方向，细节待定】

产品文档 §四 的核心机制是"AI 的产物永远先落在纸的周围"。它有两层含义，工程上是两件事：

**认知减速**（不自动落进正文）—— 已经由 §一 的写权限收窄保证。

**上下文驻留**（回答不随聊天时间线滚走）—— 需要回答卡有"归属"：

- 回答卡 → 必属于一张问题卡（`parentCardID`）；
- 问题卡 → 必属于一个锚点（`anchorID`）；
- 从资料卡长出的问题 / 批注 / 碎片 → 继承来源锚点，并各有一根 `derived` 边；
- 因此回答卡**自动获得一个正文位置**，而不需要一个聊天窗口。卡再长出卡也不许变成第二份聊天。

由此推出两条实现规则：

1. **回答卡不允许被"移动"到与它的问题无关的位置后再独立存在。** 允许作者把它拖远（工作需要空间），但归属不变，双向高亮仍然按归属工作。
2. **周围不存在"游离的对话"。** 如果一个交互产生了没有锚点的 AI 输出，那它设计错了。

这也顺带解决了产品文档 §十一 的"AI 产出堆积"风险：产物有归属，就自然会被"这一段有几张卡"的组织方式收敛，而不是堆成一条越来越长的流。

## 五、检索顺序：先自己的语料，后外部【已定方向】

产品文档 §六 明确写了"**先搜我自己的语料**，再外部"。这既是产品判断，也是工程上的成本优势：

1. `corpus.search(query)` —— 本地 FTS5，同步、免费、离线可用。命中直接出资料卡（`provenance.kind = clipped`，因为是你自己的东西）。
2. 只有在作者显式要求"往外找"时，才发起外部检索。结果进 `retrieved`。

【待定】跨文重复 / 矛盾检测需要语料索引的成熟度，产品文档把它标为"长期护城河"。本原型不做，但 FTS5 索引从第一天就建，免得将来补。

## 六、模型接入与失败姿态

| 项 | 决定 |
| --- | --- |
| API key | **应用自己持有**，直连模型厂商；不经第三方中转【已定】 |
| 传输 | 纯 HTTPS 请求，沙盒内合法；无本地进程、无 MCP 宿主 |
| 可用性 | 无网 / 无 key / 模型不可用 = **产品仍完全可用**，只是 AI 手势给不出卡。写作本身不依赖任何网络【已定】 |
| 成本控制 | 一次手势 = 一次请求 = **有上限的产物数**（建议 ≤3 张卡）。不做后台扇出 |
| 可审计 | 每张 AI 卡带 `provenance.model` + `trigger`，作者能看出这张卡是谁、什么时候、因为什么产生的 |

### 6.1 MCP 在这个产品里是什么【待定】

Mosaic 的 MCP 有明确用途：让外部 agent（Claude Code / Codex）往房间里放卡。write-field 在 iPad 上**没有宿主外部 agent 的合理场景**——沙盒、无进程、无本地监听。

所以本轮的判断是：

- **语义命令层照做**（§二），因为它是应用内 AI 功能与数据层之间正确的分界；
- **MCP 传输层先不做**。将来如果 macOS 版要接外部 agent，再把同一层包一个 MCP 面，**不重新设计操作集**。

这个判断的价值在于：不做 MCP 服务，但把"MCP 式的小而语义化的命令"这个纪律保留下来。

## 七、和产品文档的逐条对账

| 产品文档的要求 | 本文的工程对应 | 状态 |
| --- | --- | --- |
| AI 是助手不是主人；进不进正文由作者决定 | AI 无正文写操作；作者有 `insertFromCard` | 【已定】 |
| 不判断内容真伪 | 无 `truth.verify`，无真假字段，只有"从哪来" | 【已定】 |
| 不自动插入任何内容 | 唯一入正文路径是作者手势：插入块或 footnote | 【已定】 |
| 编造引用架构级禁止 | `clipped`/`retrieved`/`asserted` 判别联合 + URL 实访 + `asserted` 进不了引用位 | 【已定】 |
| AI 产物先落在卡上（含可插入的图表 / 片段） | 写操作集只有卡片与边 | 【已定】 |
| AI 默认安静，不主动扫全篇 | 无定时器 / 无观察者 / 默认读作用域 = 一个锚点 | 【已定】 |
| 先让我怀疑，再让机器核对 | AI 的唯一入口是作者手势；每卡带 `trigger` | 【已定】 |
| 先自己的语料，后外部 | `corpus.search` 在前，外部检索需显式要求 | 【已定方向】 |
| AI 产出堆积的风险 | 产物必有归属（回答 → 问题 → 锚点）；一手势产物数有上限 | 【已定方向】 |
| AI 变强会不会让它贬值 | 价值在"读、质疑、记住、保留上下文"，这些不会随模型变强而消失 | 立场，非工程项 |

## 八、明确不做（本轮）

- 不做 MCP 服务端 / 不做外部 agent 接入。
- 不做 embedding、不做跨文语义检索。
- 不做自动全篇体检、不做"重要文档"自动模式（先留接口位，不实现）。
- 不做多模型编排、不做 agent 工具循环。
- 不做任何形式的"AI 协作写作"（边写边补全）。

---

## 附：需要验证的假设（进入实现前）

1. 【待定】默认读作用域收窄到"一个锚点 + 邻段 + 已挂卡片"是否够用——会不会因为上下文不足而产出低质量批注。这是本设计里最可能被真实使用推翻的一条。
2. 【待定】一次手势 ≤3 张卡的上限是否合适。
3. 【待定】"草稿 / 待用 / 已用"三态在真实使用中是否真的会被作者维护；如果不会，`state` 就退化成了纯噪音（产品文档 §十 问题 10 也是这个疑点）。
